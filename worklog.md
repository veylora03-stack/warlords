# WARLORDS — Engineering Worklog (append-only)

---

Task ID: 0
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 0 — Architecture + Repository Structure + Database Design + API Design + Development Plan

Work Log:
- Inspected repo: Next.js 16 + React 19 + TS 5 + Tailwind 4 + shadcn/ui scaffold, Prisma 6 (SQLite sandbox driver), Zod 4 / Zustand 5 / TanStack Query 5 present; git repo with only an empty initial commit.
- Committed untouched scaffold as baseline (`chore: baseline scaffold`).
- Wrote full architecture document set under `docs/`:
  - ARCHITECTURE.md — stack decisions (incl. justified deviations: Route Handlers instead of separate NestJS/Fastify service; SQLite only as dev driver, PostgreSQL-first schema), layering rules (adapters→services→engines→config), lazy-tick game state model, BigInt/basis-point numeric policy, notification outbox, observability, deployment topology (sandbox gateway + Vercel/Supabase production), module maturity matrix.
  - DATABASE_DESIGN.md — ERD (mermaid), 45-entity catalog, ledger-first economy, catalog/instance split, hot-path indexes, transaction boundary examples, PG migration notes.
  - API_DESIGN.md — envelope contract, error code taxonomy, auth headers, idempotency, rate-limit groups, full endpoint catalog for phases 1–9, attack + idempotent-claim sequences.
  - BATTLE_MODEL.md — deterministic seeded simulation, bps modifier aggregation, data-driven counter matrix, round loop, loot/hospital/protection rules, replay contract, anti-exploit notes.
  - SECURITY.md — Telegram initData HMAC verification, JWT session, anti-cheat matrix, RBAC, anti-replay, secrets register, honest limitations register.
  - ROADMAP.md — phase contracts 0–13 with exit criteria + per-phase quality gate (lint/typecheck/runtime/commit/worklog).
- Designed complete Prisma schema (`prisma/schema.prisma`): 45 models incl. users, players, resource_wallets, resource_transactions (ledger), cities, buildings, unit_types, player_units, marches, commanders, items, technologies, territories, battles/battle_rounds/battle_logs, quests, clans & wars, market, seasons, world_bosses, events, notifications, admin_audit_logs, idempotency_keys, post-MVP-ready diplomacy_relations & spy_missions. All tables @@map'd to snake_case; BigInt for money/power/HP; Json for data-driven config blobs.
  - Fixed 4 validation errors (missing back-relations User.notifications→removed, Player.transactions & Player.ledClans added, March.battleId made unique).
  - `prisma validate` ✅ and `prisma db push` ✅ on SQLite (schema proven valid; Prisma Client regenerated). Post-MVP tables exist from day one so later phases are additive.
- Implemented Phase 0 code:
  - `src/lib/game/types/` — common.ts (all enum-like unions + bps policy), game-state.ts (projections, command intents, reconciliation contract), battle.ts (BattleConfig/BattleSide/BattleInput/simulation/replay types + mergeBps helper), index.ts barrel.
  - `src/lib/api/errors.ts` — AppError taxonomy (stable machine-readable codes) + helpers.
  - `src/lib/api/response.ts` — standard envelope, BigInt-safe JSON serialization, request-id propagation, `handle()` catch-all wrapper.
  - `src/app/api/health/route.ts` — real liveness probe (SELECT 1 via Prisma, latency, uptime, version).
  - `src/app/page.tsx` — WARLORDS dev console (dark fantasy/military theme, amber accents, no indigo/blue): live system status from /api/health, architecture summary, 14-phase roadmap, Phase 0 deliverables; sticky footer via min-h-screen flex + mt-auto; responsive.
  - layout.tsx metadata → WARLORDS; README.md; .env.example (no secrets); package.json `typecheck` script; tsconfig excludes sandbox-only dirs (examples/skills/tests).
- Quality gate: `tsc --noEmit` ✅ clean · `eslint .` ✅ clean · dev server 200s ✅ · Agent Browser verification ✅ (page renders, DATABASE UP with real SELECT 1 latency, zero console errors, zero page errors, desktop 1108px + mobile 390px screenshots verified, footer sticks/pushes correctly) · dev.log free of errors.
- Commit: `feat(phase-0): architecture, database design, api design, battle model, security model, roadmap + schema + api infra + status console`.

Stage Summary:
- Phase 0 exit criteria met: schema validates & pushes; typecheck/lint clean; `/` renders verified console; `/api/health` returns real DB probe.
- Key decisions recorded: Next.js Route Handlers as REST layer with framework-agnostic game core; PostgreSQL-first Prisma schema (SQLite sandbox only); JWT session via `jose` over next-auth for Telegram initData; lazy-tick world without cron; ledger-first economy; deterministic replayable battles with configVersion snapshots; post-MVP tables schema-ready from day one.
- Awaiting user approval to begin PHASE 1 (Database & Authentication: migrations, Telegram initData verification, session JWT, middleware, logger, rate limiter, player bootstrap).

---
Task ID: 0-b
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 0 (revision) — expand architecture into the full 12-view master specification, precise roadmap; no major implementation

Work Log:
- Re-inspected repository per instruction: clean tree at commit 9bb5825, 6 docs, api infra + types + health + console from 0-a, server healthy (db up, 1ms probe).
- Rewrote docs/ARCHITECTURE.md as v2.0 MASTER with the 12 requested views (system, frontend, backend, database, telegram, authentication, battle, economy, notification, admin, security, deployment) + folder structure + hard module/dependency boundaries + cross-cutting strategy summary table. v1.0 preserved in git history.
- New deep-dive documents:
  - FRONTEND_ARCHITECTURE.md — single-route SPA shell, component layering, TanStack/Zustand split, query-key taxonomy + invalidation matrix, serverTime-anchored timers, Telegram SDK wrapper contract, UX/perf budgets, i18n (fa/en), a11y.
  - BACKEND_ARCHITECTURE.md — runtime model, module dependency rules (engine purity contract), request lifecycle, 3-class error handling strategy, structured logging strategy, multi-layer caching strategy (Redis-ready, stampede-safe), phased WebSocket strategy (polling now, socket.io mini-service :3003 later, polling as permanent fallback), optional WORKER mode.
  - TELEGRAM_ARCHITECTURE.md — webhook+long-poll pipeline, 9 command surface, /start payload grammar (referrals/campaigns), deep-link grammar, Mini App lifecycle contract, notification delivery queue with throttling + mute prefs, Telegram-specific security rules, explicit MVP non-goals.
  - AUTHENTICATION.md — identity model, Flow A initData HMAC verification sequence (mermaid), Flow B env-guarded dev impersonation, Flow C admin login with allowlist, guard/failure code map, secrets summary.
  - ECONOMY_ARCHITECTURE.md — principles, money types, SIX canonical transaction flows with sequence diagrams (production collect, upgrade spend, training+upkeep, battle loot transfer, market escrow fill, admin adjust), invariant table + enforcement, faucets/sinks inflation model, anti-exploit map.
  - ADMIN_ARCHITECTURE.md — separate admin JWT + allowlist, console modules with safety rails, exploit-response runbook, MVP non-goals.
- BATTLE_MODEL.md extended: authoritative 12-step mermaid sequence flow (client→API→service tx→engine→apply→notify) + March state machine with CAS resolver exclusivity (sweep/reconcile/worker idempotent vs each other) + attack-incoming early warning note.
- ROADMAP.md rewritten as precise roadmap: per-phase task breakdowns with acceptance criteria, effort sizing (S/M/L), dependency graph (incl. parallelization notes: P9 core can start after P1, P8 panels ship incrementally), top-6 risk register with mitigations.
- README doc table updated to the full 12-view set (12 documents incl. API_DESIGN).
- Quality gate re-run: eslint ✓ tsc ✓ /api/health returns healthy/db up ✓. No production code changed in this revision (docs-only) per "no major implementation" instruction.

Stage Summary:
- Phase 0 now satisfies the expanded deliverable list: all 12 architectures, folder structure, module+dependency boundaries, entities+relationships (DATABASE_DESIGN ERD), initial API endpoints (API_DESIGN), WebSocket/caching/error/logging strategies, logical ERD, complete battle flow, economy transaction flows, precise roadmap.
- Consistency kept with already-merged Phase 0 code (error codes ↔ errors.ts, envelope ↔ response.ts, endpoints ↔ API_DESIGN, schema ↔ prisma/schema.prisma).
- Awaiting approval for PHASE 1 (Database & Authentication).

---
Task ID: 1
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 1a — Project Foundation (TypeScript · lint · formatter · env config · error handling · logging · API structure · frontend/backend structure · testing infra · git config · production build)

Work Log:
- Hardened tsconfig: removed `noImplicitAny:false` override (strict now fully applies), added `noImplicitOverride`, `noFallthroughCasesInSwitch`, `forceConsistentCasingInFileNames`, target ES2022.
- ESLint v2: re-enabled `@typescript-eslint/no-unused-vars` (with ^_ escape hatch) + `prefer-const`/`no-var`/`no-unreachable`; added import-boundary enforcement per ARCHITECTURE.md — UI layer (app pages/providers, components, features, stores, hooks) cannot import lib/db, lib/bot, engines, services, lib/health, config/env; `lib/game/engine/**` purity rule (no db/api/auth/bot/prisma/next-server). Fixed 3 real violations this surfaced (unused vars in page.tsx / game-state.ts; use-toast value-only const converted to type).
- Prettier 3.9.6 installed; `.prettierrc.json` (no-semi, single-quote, w100) + `.prettierignore` (docs/md/public/db/skills excluded); `format` + `format:check` scripts; whole src/tests/config tree formatted.
- Config layer: `src/config/env.ts` — Zod-validated env (DATABASE_URL required; NODE_ENV/LOG_LEVEL enums with defaults; JWT_SECRET ≥32; ADMIN_TELEGRAM_IDS parsed to number[]), pure testable `loadEnv()` + cached `getEnv()`/`getEnvSafe()`, fail-fast ConfigError with issue list; `src/config/app.ts` client-safe constants (APP_NAME/VERSION/PHASE). Updated `.env.example` with LOG_LEVEL.
- Logging: `src/lib/logger/index.ts` — structured single-line JSON contract from BACKEND_ARCHITECTURE.md (level,time,module?,requestId?,playerId?,msg,durationMs?,err), level threshold from LOG_LEVEL, child loggers, deep redaction (token/secret/password/authorization/initData/cookie/apiKey), error serializer, timer() helper, injectable sink for tests.
- Error handling upgraded: `response.ts handle()` now logs request-complete (info) with requestId/method/path/status/durationMs, expected AppError → warn, unknown → error + generic 500; adds `x-response-time-ms` header. `ErrorDetails` made recursively JSON-safe so validation issues can travel in envelopes.
- API structure: `src/lib/api/route-handler.ts` — `defineRoute({body?,query?}, fn)` uniform adapter: JSON parse → Zod validate → typed ctx → handle(); `zodErrorToAppError` (issues with path+message); `parseJsonBody` (empty→undefined, malformed→VALIDATION_ERROR). Health route + new `/api` index rebuilt on the factory.
- Backend module pattern: `src/lib/health/` (health.types.ts + health.service.ts + barrel) as the reference module; `/api/health` is now a thin adapter; version/phase constants come from config (no hard-coded values in routes).
- Frontend structure: `src/app/providers.tsx` (TanStack Query with sensible defaults, wired into layout); `src/features/system/` slice (types + useHealthQuery with 15s polling + barrel); `src/stores/ui.store.ts` (Zustand auto-refresh slice); `src/types/api.ts` type-only re-exports; console page refactored off useEffect-fetch onto the feature hook + store (real wiring, no dead code).
- Testing infra: `tests/unit/` — 25 tests across logger (levels/bindings/redaction/timer), errors (status map/factory), env (valid/invalid/parse/freeze), route-handler (zod mapping, JSON parse). `tests/e2e/api-smoke.test.ts` — real-server contract tests (envelope shape, x-request-id, db probe, `/api` index, page HTML) with top-level-await reachability probe (describe.skipIf captures at registration — beforeAll was too late; found & fixed).
- Git hygiene: `.gitattributes` (eol=lf normalization, binaries, lockfile -diff); **untracked `.env` and `db/custom.db` which were previously committed** (secret-hygiene violation — user rule "no secrets in repo"); added `/db/*.db*` ignores.
- Production build: removed `typescript.ignoreBuildErrors` from next.config.ts (build now typechecks for real), reactStrictMode on; `bun run build` green; standalone server smoke-tested on :3100 (health db up, /api index, / 200) then shut down; dev server restarted.
- Scripts finalized: dev, build, start, test (bun test tests/unit/), test:e2e, lint, typecheck, format, format:check (+ db:* kept). package.json renamed to `warlords`@0.2.0.
- Docs synced: ROADMAP.md — new `PHASE 1a — Project Foundation ✅` contract table, old Phase 1 → `PHASE 1b`, quality gate extended with build+tests; ARCHITECTURE.md boundary note marked enforced; README status + scripts + source-layout updated.
- Quality gate (all green): format:check ✓ · eslint ✓ · tsc ✓ · unit 25/25 ✓ · e2e 3/3 ✓ · `next build` ✓ (typed, standalone) · dev.log zero errors · Agent Browser: desktop + mobile 390px rendered, DATABASE UP real latency, auto-refresh toggle OFF→"UP · PAUSED" (polling stops) ON→"DATABASE UP" (polling resumes, fresh requests in dev.log), sticky footer OK, zero console/page errors.

Stage Summary:
- Phase 1a exit criteria met: project foundation is production-grade — strict TS, boundary-enforcing lint, formatter, Zod env config layer, structured logging, uniform route factory, module pattern, feature/store-based frontend skeleton, unit+e2e infrastructure, hardened git, and a typechecked standalone production build.
- Key decisions: zero new runtime deps (logger & tests are bun-native); bun test instead of vitest (tests/ excluded from tsc, transpiled by bun); `handle()` logs every request (ops-ready JSON in dev.log); `.env` untracked from history going forward (file removed from index only — content was a local sqlite path, no live secrets).
- Awaiting approval for PHASE 1b (Database & Authentication): x-request-id middleware + security headers, baseline migration, initData HMAC verify, session JWT, auth endpoints, rate limiter, catalog seeds, player bootstrap.

---
Task ID: 2
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 2 — Database Foundation (PostgreSQL-first schema → migration → seeds → transactional bootstrap → invariant verification)

Work Log:
- Audited the 45-model Phase 0 schema against the user's 31-model contract. Gap analysis: 25 existed as-is; 6 table renames applied (resource_wallets→resources, unit_types→units, inventory_items→inventory, leaderboard_snapshots→leaderboards, game_events→events, admin_audit_logs→audit_logs); 1 new model (AdminUser → admin_users, explicit admin registry with identity kept on User); model rename UnitType→Unit with FK field unitTypeId→unitId rippled through PlayerUnit/TrainingQueueItem.
- Relation hygiene sweep (all documented in schema header policy): explicit onDelete everywhere — Cascade for owned instance data (units/quests/buildings/notifications/memberships), Restrict for history & ledgers (battles attacker, market orders/transactions, audit actor) and for catalog FKs (unit/item/tech/quest/commander/achievement — soft-disable via isActive, never hard delete), SetNull for optional soft refs (battle defender/territory/boss, march target, territory owner/city, clan-war winner, event target). Fixed one semantic bug: Battle.attacker was Cascade (would destroy battle history on player delete) → Restrict.
- Timestamps: createdAt on every table; updatedAt (@updatedAt) added to all 13 mutable tables that lacked it (PlayerQuest, TrainingQueueItem, March, Battle, ClanMember, ClanWar, MarketOrder, Season, Leaderboard, WorldBoss, GameEvent, Announcement, catalogs, Territory, Clan, CommanderEquipment).
- Indexes added: units(class,tier), technologies(branch,tier), quests(type,sortOrder), player_units(unitId), territories(ownerPlayerId), marches(territoryId), market_transactions(buyer/sellerPlayerId+createdAt), leaderboards(seasonId), seasons(status,endsAt), events(targetPlayerId), players(seasonPoints).
- Baseline migration cut on a fresh dev DB: prisma/migrations/20260829175821_baseline/migration.sql (committed); `prisma migrate dev` + generate green; scripts db:migrate / db:migrate:deploy added, db:push removed (migrate is canonical now).
- Data-driven content layer created — src/lib/game/config/: units.ts (6 units T1–T2, counter triangle Infantry▸Cavalry▸Ranged▸Infantry, 2000–2500 bps), technologies.ts (4 techs incl. prerequisite chain iron_sharpening→advanced_steel), quests.ts (MAIN×3 chain + DAILY×2 repeatables, typed OBJECTIVE_TYPES/REWARD_KEYS), achievements.ts (×4), items.ts (×4), starter.ts (starter wallet/energy/17 buildings/20 militia+10 archers/starter quests/Season 1/dev fixtures), barrel index.ts. Zero balance numbers in logic.
- Transactional sensitive-operation reference implementation: src/lib/game/services/player-bootstrap.service.ts — bootstrapPlayer(tx, …) creates player + wallet + 5 ledger faucet rows (BOOTSTRAP reason, balanceAfter chain) + city + 17 starter buildings + starter army + starter quests (targets read from DB catalog inside tx, hard-fails if catalogs missing) + welcome notification in ONE transaction. Caller owns the tx; seed uses it today, auth reuses it next phase.
- Idempotent seed pipeline: prisma/seed.ts (db:seed + prisma.seed config) — catalog upserts from config, Season 1 ACTIVE, dev admin (env ADMIN_TELEGRAM_IDS precedence), 2 dev players via bootstrapPlayer. Proven idempotent: second run bootstraps 0 players.
- DB invariant verifier: scripts/db-verify.ts (db:verify) — V1 ledger Σdelta==wallet per resource + balanceAfter running-chain check, V2 per-player completeness (wallet/city/17 buildings/ledger/quests/army), V3 in-DB config reference integrity (unit counters, quest prereqs), V4 catalogs non-empty, V5 coordinate uniqueness. Result on seeded DB: ✓ all hold, ledger reconciles exactly.
- Tests: tests/unit/config.test.ts — 20 invariant tests over the config catalogs (unique ids, positive costs/stats, valid enums, counter/prereq reference resolution incl. cycle detection, MAIN chain ordering, starter kit consistency). Suite now 45/45 green.
- Consistency fixes found during review: Player.leaderboardSnapshots type updated to Leaderboard[]; stale comments (unitTypeId) updated; seed JSON fields bridged to Prisma InputJsonValue via typed json() helper; removed a dead variable flagged by lint in db-verify.
- Console/docs: page.tsx phase list renumbered to user phases (00/01/02 COMPLETE, "03? Auth & Player Bootstrap" NEXT) — caught & fixed a duplicate React key bug introduced by the renumber (all planned rows shared id '—'); browser-verified zero console errors after fix. ROADMAP.md: PHASE 2 contract table + old 1b retitled "PROPOSED NEXT (awaiting user phase number)". README: status/scripts/structure. DATABASE_DESIGN.md: implemented-section with 31-table mapping, cascade policy, migration & tx patterns.
- Quality gate: prisma validate ✓ · migrate dev ✓ · generate ✓ · db:seed ✓ (idempotent) · db:verify ✓ · tsc ✓ · eslint ✓ · prettier ✓ · 45 unit ✓ · 3 e2e ✓ · next build ✓ (typed, standalone) · dev server healthy (db up, v0.3.0-phase2) · Agent Browser desktop+mobile ✓ zero console errors.

Stage Summary:
- Phase 2 exit criteria met: the 31-table contract is migrated, seeded, invariant-checked, and proven in a production build; PostgreSQL remains the deploy target (SQLite = sandbox driver only; PG baseline procedure documented in DATABASE_DESIGN.md).
- Key decisions: table names = user contract, Prisma model names = domain-clear; Restrict-vs-Cascade semantics protect history; catalogs soft-disable (isActive) and are never referenced with Cascade; bootstrap service is THE single transactional player-creation path (seed + future auth); balance lives only in src/lib/game/config.
- Awaiting approval for the next phase (proposed: Authentication & Player Bootstrap — initData HMAC, session JWT, auth endpoints, rate limiter, middleware).

---
Task ID: 3-i
Agent: Z.ai Code (doc-sync sub-agent — Task 3-i)
Task: PHASE 3 — documentation sync (AUTHENTICATION/SECURITY/API_DESIGN/ROADMAP/DATABASE_DESIGN/README)

Work Log:
- Read worklog (Phases 0/1a/2) + all Phase 3 sources to ground the docs in code: src/lib/telegram/init-data.ts, src/lib/auth/* (session.config/jwt/cookies/hash/session.types/session.service/guard), src/lib/rate-limit/index.ts, the four /api/v1/auth/* routes, prisma/schema.prisma (AuthSession), src/config/env.ts, and the unit/integration/e2e test files.
- Verified counts by execution: unit 84/84 ✓ (bun run test), integration 16/16 ✓ (bun run test:integration); e2e = 7 (4 auth-smoke + 3 api-smoke) confirmed by file inspection.
- docs/AUTHENTICATION.md — rewritten to implemented state: identity table now says JWT + auth_sessions row (sha256 tokenHash, revocable, 48h sliding refresh, Bearer-or-wl_session); Flow A mermaid matches the real sequence (rate limit 10/min per IP BEFORE crypto → HMAC("WebAppData", bot token) → freshness ≤ TELEGRAM_AUTH_MAX_AGE_SECONDS w/ 300s skew → ONE tx: upsert→ban check→expired-session cleanup→replay resolution→session row→bootstrapPlayer); documented the replay policy (idempotent re-attach to the same row via unique initDataHash + token rotation, replayed:true, fresh initData mints a new session); Flow B rewritten with the exact guard chain (5/min rate limit → prod 404 → ADMIN_SECRET 503/401 constant-time → allowlist 403 → audited session, synthetic initDataHash dev-impersonate:{telegramId}); Flow C marked "designed — Phase 9"; Guards & Failure Map table rebuilt from actual codes (INVALID_INIT_DATA/UNAUTHORIZED/SESSION_EXPIRED/SESSION_REVOKED/BANNED/RATE_LIMITED/AUTH_NOT_CONFIGURED/NOT_FOUND/FORBIDDEN) incl. INVALID_INIT_DATA details.reason enum; §6 secrets/tunables updated (prod-required secrets, TELEGRAM_AUTH_MAX_AGE_SECONDS 86400, SESSION_TTL_SECONDS 604800).
- docs/SECURITY.md — §1.1 verification steps rewritten to the code order (bounds → signature FIRST → freshness/skew → user shape after signature; single 401 code with details.reason for logs/tests, never attacker guidance); §1.2 session section now documents JWT+row pair (sid → row authority, raw token/initData never persisted, sha256 only), revocation via logout, sliding refresh at 48h, Bearer precedence, no edge middleware rationale; §2 rate-limit layer marked implemented (in-memory sliding window, Redis-ready RateLimitStore, shared per-process, enforced before verification); §5 added non-secret tunables table (TELEGRAM_AUTH_MAX_AGE_SECONDS / SESSION_TTL_SECONDS); §6 anti-replay now leads with the initData replay decision and the WHY (retry-safety, no session farming, immediate old-token death, exposure bounded by initData freshness — residual stolen-initData risk documented honestly); §8 limitations updated (per-process limiter + requireAuth-over-edge-middleware rationale).
- docs/API_DESIGN.md — §1.2 taxonomy synced to errors.ts (added SESSION_REVOKED, AUTH_NOT_CONFIGURED 503 group, generic NOT_FOUND, RATE_LIMITED moved to a 429 Throttling row); §1.3 header guidance fixed: initData travels in the POST body of /auth/telegram (x-telegram-init-data header removed), Authorization: Bearer <session JWT> or wl_session cookie for protected routes (Bearer wins); §2.2 replaced with the four real endpoints (telegram/me/logout/dev-impersonate) with one-line purposes + failure-code footer.
- docs/ROADMAP.md — inserted "PHASE 3 — Telegram Authentication ✅ (current)" contract table (3.1–3.8: init-data crypto, auth module, transactional session service, 4 routes, requireAuth/no-edge-middleware decision, rate limiter, env tunables, tests incl. the 7 attack scenarios) + replay-policy paragraph + quality gate line (lint ✓ typecheck ✓ unit 84 ✓ integration 16 ✓ e2e 7 ✓ build ✓); old "PROPOSED NEXT — Authentication & Player Bootstrap" replaced by the delivered table with deferred items noted (Mini App shell v0, GET /api/v1/player/me); original-numbered plan block annotated as superseded.
- docs/DATABASE_DESIGN.md — IMPLEMENTED section: auth_sessions row added to the table-name contract (initDataHash unique, tokenHash unique, userId FK Cascade, telegramAuthDate/issuedIp/userAgent/lastUsedAt/expiresAt/revokedAt, (userId,expiresAt) index) + "Session semantics" paragraph (one live session per initData, rotation, per-request row re-read = DB authority); bootstrapPlayer note updated (consumed by seed AND Phase 3 auth first login).
- README.md — status → "Phase 3 — Telegram Authentication: COMPLETE"; Getting Started fixed (db:push → db:migrate + db:seed, db:push was removed in Phase 2); Scripts table gained test:integration; source layout now lists lib/auth, lib/telegram, lib/rate-limit, /api/v1/auth/*, and tests/unit+integration+e2e.
- .env.example — verified: already contains TELEGRAM_AUTH_MAX_AGE_SECONDS="86400" and SESSION_TTL_SECONDS="604800" under "Auth tunables (Phase 3)" — no change needed.
- No source code touched (docs/README/worklog only). Sanity: tsc --noEmit clean; format:check/lint issues confirmed pre-existing in Phase 3 test sources (see Stage Summary), not from this task.

Stage Summary:
- Documentation now matches the Phase 3 implementation exactly: flow sequences, guard chain, error codes, endpoint catalog, session/replay semantics, DB contract and test counts are code-grounded (no invented endpoints or fields).
- Replay policy is documented as a first-class design decision in three places (AUTHENTICATION §2, SECURITY §6, ROADMAP Phase 3) with rationale: retry-safety + no session farming + immediate old-token invalidation, exposure bounded by TELEGRAM_AUTH_MAX_AGE_SECONDS.
- Docs-only task; zero risk to the build. Recommendation for the next agent before commit: fix 2 pre-existing lint errors in Phase 3 test sources (tests/integration/auth/auth-flow.test.ts:99 validTgId let→const; tests/unit/auth/jwt.test.ts:72 unused 'payload' destructure → rename/prefix _) so the recorded "lint ✓" gate holds on re-run; 9 pre-existing prettier format:check warnings in test/source files likewise predate this task.

---
Task ID: 3
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 3 — Telegram Authentication (initData validation · signature/hash · expiration · identity extraction · secure sessions · authorization middleware · 7 attack scenarios)

Work Log:
- Audited Phase 0/2 auth contracts (AUTHENTICATION.md, SECURITY.md) before writing code; found and repaired a garbled schema line report (the `@@index([honor])` "corruption" was a flaky tool-read artifact — verified via prisma validate + migrate diff that the repo was clean; no code change was needed for it).
- Schema: added `AuthSession` (auth_sessions) — initDataHash unique, tokenHash unique, userId FK Cascade, (userId, expiresAt) index, revokedAt; migration `20260829190046_add_auth_sessions` cut via `prisma migrate dev`, client regenerated.
- Config: env.ts gains TELEGRAM_AUTH_MAX_AGE_SECONDS (default 86400) + SESSION_TTL_SECONDS (default 604800); JWT_SECRET + TELEGRAM_BOT_TOKEN now REQUIRED in production (superRefine, fail-fast) while staying optional in dev (auth endpoints → AUTH_NOT_CONFIGURED 503). errors.ts gains SESSION_REVOKED 401, AUTH_NOT_CONFIGURED 503, generic NOT_FOUND 404.
- src/lib/telegram/init-data.ts — pure, Telegram-official verification: data_check_string (all fields except hash, sorted, \n-joined), secret = HMAC("WebAppData", bot token), expected hash constant-time compared; auth_date freshness + 300s clock-skew bound; user JSON shape validation (safe-int positive id, name/username/language caps, https-only photo_url, strict booleans); single public code INVALID_INIT_DATA with granular internal `details.reason`.
- src/lib/auth/ — HS256 JWTs via jose (sub/sid/role; role+ban ALWAYS re-read from DB per request), wl_session HttpOnly SameSite=Lax cookie (Secure in prod) + Bearer for native contexts; session service runs ONE transaction per login: user upsert (telegramId = THE identity) → ban check → expired-session cleanup → replay resolution → session row (uuid id, sha256 token hash — raw tokens never stored) → FIRST-LOGIN player bootstrap via the Phase 2 bootstrapPlayer (wallet + ledger + city at free spiral coords + 17 buildings + army + quests + welcome notification) → optional audit row.
- REPLAY POLICY (tested, documented): identical initData re-attaches to the SAME auth_sessions row (unique initDataHash) and ROTATES the token hash — no session farming, old token dead immediately, client network-retries never lock users out; fresh initData (new auth_date) mints a new session.
- Authorization middleware: `requireAuth(request, {refresh})` composes into routes (no edge middleware.ts — Prisma/ban check must run in Node runtime); credential order Bearer → cookie; DB row is revocation/token-hash authority; sliding refresh re-issues inside 48h-of-expiry window; throttled lastUsedAt.
- Dev impersonation (Flow B): dev-only (404 in prod), rate-limited 5/min per IP BEFORE secret check, ADMIN_SECRET constant-time compare, ADMIN_TELEGRAM_IDS allowlist, fully audited (audit_logs DEV_IMPERSONATE).
- Rate limiting: src/lib/rate-limit in-memory sliding window w/ Redis-ready RateLimitStore interface; auth group 10/min per IP enforced before any crypto/DB work; 429 + retryAfterSec.
- Routes: POST /api/v1/auth/telegram, GET /api/v1/auth/me (protected, introspection + refresh), POST /api/v1/auth/logout (revokes row + clears cookie), POST /api/v1/auth/dev-impersonate; /api index lists the v1 surface.
- Frontend: features/auth slice (useMeQuery — 401 mapped to anonymous state, never an error) + Session card on the console (ANONYMOUS ↔ SIGNED IN with identity/player/expiry/role); phase list + deliverables + footer updated to Phase 3.
- Tests (107 total green): unit 84 — initData vectors signed with the REAL algorithm (valid/tampered/wrong-token/expired/future-skew/missing fields/user-shape), limiter sliding-window semantics, JWT round-trip/expiry/foreign-secret/tamper/issuer; integration 16 — the 7 required scenarios through real route handlers + DB: valid auth (user+session+player in one tx, cookie issued), invalid hash 401, expired 401 (reason=expired), missing (400 body / 401 me), manipulated user 401, replay idempotency (same session, no farming, fresh initData → new session), unauthorized API access (garbage/foreign-secret/revoked tokens, ban enforcement 403 with tx rollback + unban recovery), Flow B guards + audit; e2e 7 — live-server guard contract + full flow exchange→me→logout→me-dies + tampered-over-HTTP.
- Environment hygiene: fresh random JWT_SECRET/ADMIN_SECRET generated into local .env (gitignored, verified); .env.example documents both new tunables; no secrets anywhere in tracked files.
- Docs synced by subagent Task 3-i: AUTHENTICATION.md (implemented-state Flow A/B + replay policy + failure map), SECURITY.md (session + anti-replay + tunables), API_DESIGN.md (real §2.2 endpoint table, body-based initData), ROADMAP.md (Phase 3 contract table), DATABASE_DESIGN.md (auth_sessions), README (status/scripts).
- Quality gate (all green): eslint ✓ · tsc --noEmit ✓ · prettier ✓ · unit 84/84 ✓ · integration 16/16 ✓ · e2e 7/7 ✓ · next build ✓ (typed; all 4 auth routes dynamic) · dev.log free of unexpected errors (only expected 401/403 warnings from negative tests) · Agent Browser: page renders zero page-errors, ANONYMOUS → dev-impersonate → SIGNED IN golden path verified with real crypto+DB, identity/player/role/expiry rendered, desktop 1280px + mobile 390px screenshots checked, footer sticky behavior unchanged.

Stage Summary:
- Phase 3 exit criteria met: Telegram Mini App identity is verified server-side with the official algorithm; sessions are DB-backed (revocable, replay-safe, token-hash-only storage); every protected route enforces DB-fresh ban/role state via the composable guard; first login bootstraps full player state transactionally; secrets are env-only and prod-required.
- Key decisions: DB session row + JWT hybrid (JWT signature fast-path, row = revocation/token authority); replay = idempotent re-attach + token rotation (retry-safe, anti-farming) — documented in SECURITY.md; role claim in JWT is observability-only, authorization always re-reads DB; no edge middleware (Prisma ban check per request).
- Awaiting approval for the next phase (proposed: Phase 4 — Player, Resources & Economy Core: /player/me projection, lazy-tick collect, ledger-enforced spend).

---
Task ID: 4-docs
Agent: Z.ai Code (doc-sync sub-agent — Task 4-docs)
Task: PHASE 4 — documentation sync (ROADMAP/API_DESIGN/DATABASE_DESIGN/ARCHITECTURE/README/AUTHENTICATION)

Work Log:
- Read the worklog (Phases 0–3) and grounded every statement in the Phase 4 sources before editing: config/{leveling,power,energy,stats}.ts, services/{player-registration,progression,power,stats,energy,player-state,player-bootstrap}.service.ts, auth/{session.service,guard}.ts, the three /api/v1/player/* routes, /api index, features/player (types + 3 hooks), page.tsx card, config/app.ts (0.5.0-phase4), and the test files. Verified counts by inspection: unit 119 (35 new: leveling 13 · power 10 · energy 6 · stats 6), integration 40 executed (player-system.test.ts declares 20 `it`s, two of which loop ×3 over the player paths = 24; + 16 auth), e2e 10 in 3 files (player-smoke = 3 tests). Verified starter power math from code: militia 75/unit ×20 + archer 63/unit ×10 = 2130 army; 17 building weights sum 1680; total 3810 (asserted in unit power.test.ts:85 and integration player-system.test.ts:244).
- docs/ROADMAP.md — inserted the "PHASE 4 — Player System ✅ (current)" contract table (4.1–4.8: leveling curve LEVELING{maxLevel 30, baseXp 100, growthBps 1200} + derived tables + pure functions; power-from-state with exact bps weights and recalculatePlayerPower as the ONLY write path; statistics catalog 12 counters / 3 categories with normalize-on-read + append-only writes; lazy-tick energy {max 100, +1/300s} with partial-tick carry + cap re-anchor; race-safe registration (ensurePlayer idempotent, P2002 re-attach, P1008/BUSY transient retry, withRegistrationLock, REGISTRATION_TX_OPTIONS 10s/20s) wired into issueSession; 3 profile endpoints behind requirePlayer with fresh-power + BigInt-as-string notes; bootstrap zero-fill stats + initial power + frontend slice; test breakdown) + starter-power-baseline paragraph + quality-gate line (lint ✓ typecheck ✓ format ✓ unit 119 ✓ integration 40 ✓ e2e 10 ✓ build ✓ browser-verified). Removed the now-stale "(current)" marker from Phase 1a/2/3 headers; updated the deferred-items note (player/me projection landed as the three /player/* endpoints; Mini App shell v0 still open).
- docs/API_DESIGN.md — §2.3 retitled "Player — Phase 4 ✅ (profile surface implemented)" and rebuilt from the real routes: profile/statistics/state rows with actual DTO content (xpIntoLevel/xpForNextLevel/levelProgressBps, powerBreakdown, energyNextRegenAtMs, wallet strings, army roster, buildingCount); auth = Bearer-or-wl_session via requirePlayer; serialization notes (BigInt → strings, power computed FRESH per request, lazy-tick energy); failure codes UNAUTHORIZED 401 / PLAYER_NOT_FOUND 404; kept the not-yet-implemented rows (public /player/:id, /player/me/transactions) explicitly marked "Still planned".
- docs/DATABASE_DESIGN.md — IMPLEMENTED section: players row now notes stats-JSON-by-catalog + power-as-derived-cache; new "Player-row semantics (players, Phase 4 — no new tables)" paragraph (stats = typed counter store governed by config/stats.ts with normalize rules + bootstrap zero-fill; power = derived cache written only by recalculatePlayerPower, heals on recalc, fresh value returned by endpoints) and new "Registration concurrency semantics (Phase 4)" paragraph (Player.userId UNIQUE + ensurePlayer idempotency + in-process lock + bounded retry under REGISTRATION_TX_OPTIONS); bootstrapPlayer paragraph updated (zero-filled typed stats, final initial-power recalculation, consumed via ensurePlayer).
- docs/ARCHITECTURE.md — added a Status line to the header (Phases 0·1a·2·3·4 delivered, ROADMAP contract tables = source of truth); View 3 gained the balance-surface bullet naming the new config modules (leveling/power/energy/stats) and declaring lazy-tick energy the first live implementation of decision D4; folder-structure comments updated (game/config and game/services contents; /api/v1 auth+player marked delivered).
- README.md — status → "Phase 4 — Player System: COMPLETE" with the phase chain; added a Live API line listing all eight live endpoints (incl. the three GET /api/v1/player/*) + /api index; test:integration description now covers auth + player; source layout updated (config/services contents incl. leveling·power·energy·stats, /api/v1/player/*, unit/integration/e2e test scopes); Principle 4 extended to name the progression config as part of data-driven balance.
- docs/AUTHENTICATION.md — first-login touch-ups only: §1 Game persona row → bootstrapped via ensurePlayer (idempotent, race-safe); Flow A mermaid tx step now shows ensurePlayer + initial power; one brief Phase 4 note bullet on the registration-locked transaction (withRegistrationLock + withWriteRetry + REGISTRATION_TX_OPTIONS, Player.userId UNIQUE ⇒ a retried or concurrent login can never fork a second player). All other sections untouched.
- .env.example — verified: Phase 4 added NO new env vars (leveling/power/energy/stats tunables deliberately live in typed config, not env); left unchanged.
- No source code, tests, or package.json touched; no server/test/build runs (docs + worklog only). Sanity: prettier --check passes on the edited docs (docs/*.md are outside the Prettier scope by .prettierignore, confirmed clean).

Stage Summary:
- All six docs now match the Phase 4 implementation exactly: every config number (LEVELING 30/100/1200, ENERGY 100/1/300s, POWER bps weights, 12 stat keys, tx bounds 10s/20s, retry delays), every endpoint, every failure code and the 3810 = 2130 + 1680 starter-power baseline are code-grounded — nothing invented.
- Two cross-doc decisions recorded: (1) the originally planned GET /player/me is documented as superseded by the profile/statistics/state trio in both ROADMAP and API_DESIGN; (2) API_DESIGN §2.3 now separates the implemented surface from the still-planned ledger/public-profile rows so the catalog stays honest.
- Docs-only change-set, zero build risk; ready for the orchestrator to commit together with the Phase 4 implementation.

---
Task ID: 4
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 4 — Player System (first-login chain · level/XP data-driven · derived power · statistics · energy · Profile APIs · duplicate/concurrent registration · full tests)

Work Log:
- Inspected the repo before coding (worklog Phases 0–3, schema, services, tests): the Telegram→User→Player→City→wallet chain already existed transactionally from Phase 3; Player already carried level/xp/power/honor/reputation/energy/stats columns → NO migration needed. Phase 4 = the system around them.
- Data-driven config layer (src/lib/game/config/): leveling.ts (LEVELING{maxLevel 30, baseXp 100, growthBps 1200} → derived XP_REQUIRED_PER_LEVEL / CUMULATIVE_XP_BY_LEVEL / MAX_TOTAL_XP + pure resolveLevelProgress/applyXpGain), power.ts (bps weights: unit stats+tier bonus, 17 building weights, 5 tech-branch weights; pure per-entity functions, integer math only), energy.ts (ENERGY{max 100, +1/300s} + computeEnergyState lazy-tick with partial-tick carry and cap re-anchor), stats.ts (12-counter STAT_DEFINITIONS catalog + emptyPlayerStats). Barrel updated.
- Services: player-registration.service.ts = THE registration path — sanitizePlayerName, ensurePlayer (idempotent fast-path read → bootstrap → in-tx P2002 re-attach), isUniqueConstraintError/isTransientWriteError (incl. Prisma P1008), bounded withWriteRetry, withRegistrationLock (in-process promise-chain mutex against SQLite write-lock queuing), runRegistrationTransaction with REGISTRATION_TX_OPTIONS{maxWait 10s, timeout 20s}; progression.service.ts grantXp (tx-scoped, INVALID_AMOUNT guards, level-up RANK_CHANGE outbox notification, level derived from authoritative xp column); power.service.ts computePlayerPower (DB catalogs: player_units×units, buildings, player_technologies×technology) + recalculatePlayerPower — the ONLY write path for Player.power; stats.service.ts (normalize-on-read vs catalog — unknown/missing/corrupt → safe defaults; append-only positive-integer deltas); energy.service.ts syncPlayerEnergy (lazy persist); player-state.service.ts (getPlayerProfile/Statistics/State DTOs — BigInt→string, power computed FRESH per request, energy synced).
- Bootstrap extended (player-bootstrap.service.ts): zero-filled typed stats on creation + final recalculatePlayerPower — starter power is exact (army 2130 + buildings 1680 = 3810).
- Auth wiring: session.service issueSession now runs inside withRegistrationLock → withWriteRetry → tx(REGISTRATION_TX_OPTIONS); first login delegates to ensurePlayer; P2002 at commit → RegistrationRaceError → clean whole-tx retry. New requirePlayer guard (404 PLAYER_NOT_FOUND) exported from the auth barrel.
- Profile API: GET /api/v1/player/{profile,statistics,state} behind defineRoute + requirePlayer (+sliding refresh Set-Cookie parity with /auth/me); /api index lists them; APP_VERSION → 0.5.0-phase4.
- Fixed during dev: MAX_TOTAL_XP off-by-one (cumulative[maxLevel-1]); Prisma Json fields must store objects not stringified strings; empty-interface lint error; P1008 tx-timeout race → lock+options; test fixture bug (createUser returns {userId}).
- Tests (all green): unit 119 total (+35: leveling curve/boundaries/cap, power formula exactness incl. starter 3810, energy whole/partial/cap/determinism, stats normalization tamper-proofness); integration 40 total (+24 in tests/integration/player: 401 guard matrix, first-login chain exact values, duplicate registration idempotency (no double faucet, single city/wallet), 6× parallel ensurePlayer → ONE player, 5× parallel first logins over routes → all 200 + ONE player, XP boundary/cap/notification, invalid grants rejected, statistics write validation, energy lazy regen + partial-tick carry + cap); e2e 10 total (+player-smoke: live 401s + full exchange→profile→state→statistics flow). Handler-surface test asserts only GET is exported (no client write path for power).
- Frontend: features/player (types + 3 query hooks, 401→null) + console "Player System" card — level/XP progress bar, power with breakdown, energy bar, city, wallet chips, army/building counts, lifetime stats; roadmap/deliverables/footer updated to Phase 4.
- Quality gate (all green): eslint ✓ · tsc ✓ · prettier ✓ · unit 119 ✓ · integration 40 ✓ · e2e 10 ✓ · next build ✓ (typed; player routes dynamic) · dev.log clean · Agent Browser: anonymous → dev-impersonate → SIGNED IN, Player System card LIVE with real computed values (power 3810 = 2130+1680, level 1, xp 0/100, energy 100/100, city (0,0), wallet chips, 30 units · 17 bld), desktop 1280 + mobile 390 screenshots verified, sticky footer intact, zero console/page errors.
- Docs synced by subagent Task 4-docs (ROADMAP contract table, API_DESIGN §2.3, DATABASE_DESIGN player-row/concurrency semantics, ARCHITECTURE status + balance surface, README, AUTHENTICATION ensurePlayer note).

Stage Summary:
- Phase 4 exit criteria met: the full first-login chain (Telegram User → User → Player → City → Initial Resources) is transactional, idempotent, and race-safe; level is derived from XP through a config-driven curve; power is computed from real state with a single server-side write path (client-settable power is structurally impossible); statistics and energy are typed, validated, and lazily maintained; the three Profile endpoints serve the Mini App contract with BigInt-as-string serialization.
- Key decisions: config-file curves (not DB catalogs) for leveling/power/energy — balance in one folder, runtime reads DB catalogs only for unit/tech stats; in-process registration lock + generous interactive-tx bounds to make SQLite dev concurrency deterministic (UNIQUE constraint remains the universal backstop for PostgreSQL); power recomputed fresh on every projection while the column stays a healing cache.
- Awaiting approval for PHASE 5 (proposed: City & Buildings — upgrade queue, construction timers, warehouse capacity).

---
Task ID: 5-docs
Agent: Z.ai Code (doc-sync sub-agent — Task 5-docs)
Task: PHASE 5 — documentation sync (ROADMAP/API_DESIGN/ECONOMY_ARCHITECTURE/DATABASE_DESIGN/ARCHITECTURE/README)

Work Log:
- Read the worklog (Phases 0–4) and grounded every statement in the Phase 5 sources before editing: src/lib/game/config/economy.ts (RESOURCE_CAPS BigInt literals GOLD 5e9 / WOOD·IRON·FOOD 2.5e9 / CRYSTAL 1e9 / GEMS 1e8, MAX_DELTA 1e15, ECONOMY_HISTORY {25, 100}, GRANT_IDEMPOTENCY_TTL_SECONDS 86400, LEDGER_REASONS ×8, creditWithCap/debitBalance/isEconomyDelta/isPositiveAmount), src/lib/game/services/economy.service.ts (applyResourceDeltas validate-ALL-then-write, conditional compare-and-decrement via updateMany where field >= amount on wallet AND Player.gems, skipped-at-cap credits write no ledger row, grantResources sha256 requestHash + same-tx IdempotencyKey + replayed:true + P2002 → IDEMPOTENT_REPLAY, spendResources, runEconomyTransaction withKeyLock('wallet:{id}') → withWriteRetry → ECONOMY_TX_OPTIONS {10s, 20s}, adminAdjustResources with audit_logs ADJUST_RESOURCES row + metadata {actorUserId, note}, getWalletView, getTransactionHistory keyset cursor "<ISO>|<id>"), src/lib/concurrency/mutex.ts (FIFO, error-isolated, idle-key eviction, not reentrant), both GET routes (Zod history query, requirePlayer + sliding-refresh Set-Cookie), /api index entries, tests (unit 20: config invariants/helpers/predicates/mutex; integration 25 over routes+DB in the isolated 9100005… telegramId range: the six required scenarios + audited ADMIN_ADJUSTMENT + history API; e2e 3: 401 matrix, /api index, positive flow gated on BOT_TOKEN), features/economy (types + 2 hooks, 401→null, staleTime 5s), page.tsx "Resource & Economy Engine" card, src/config/app.ts (0.6.0-phase5, APP_PHASE 5). Verified counts against the executed quality gate reported for Phase 5: unit 139, integration 65, e2e 13.
- docs/ROADMAP.md — inserted the "PHASE 5 — Resource & Economy Engine ✅ (current)" contract table (5.1–5.8 exactly as delivered: data-driven economy config · ledger-first write path · idempotent grants · race-safety layers incl. the honest in-process-mutex scope + PostgreSQL FOR UPDATE/advisory-lock note · audited admin adjustments · read APIs with the full query/DTO contract · frontend slice + console card · tests with the six scenarios and counts) + an "Economy invariants (enforced in code, proven by tests)" paragraph (no-negative via validate-then-write + conditional decrement, overflow-clamped with Σdelta==balance, every delta has a closed-catalog reason, all-or-nothing, deliberate GET-only HTTP surface) + a quality-gate line (lint ✓ typecheck ✓ format ✓ unit 139 ✓ integration 65 ✓ e2e 13 ✓ build ✓ browser-verified desktop 1280px + mobile 390px) + "Awaiting approval for PHASE 6 — City & Buildings". Removed the stale "(current)" marker from the Phase 4 header; updated the superseded-plan note so the original "Phase 2" block's ledger/economy core is marked as landed in Phase 5.
- docs/API_DESIGN.md — §2.3 retitled "Player — Phase 4 ✅ (profile surface) · Phase 5 ✅ (economy read surface)"; added §2.3.1 "Economy — Phase 5 ✅ (wallet + ledger read surface, read-only)" rebuilt from the real routes: both endpoints with actual DTO content ({playerId, resources[{key,balance,cap,headroom}]×6 canonical order, updatedAt} and {entries[{id,resource,delta,balanceAfter,reason,refType,refId,metadata,createdAt}], nextCursor, hasMore}), query contract table (limit 1–100 default 25, cursor "<ISO createdAt>|<id>", reason ∈ the 8-value catalog), serialization notes (all amounts BigInt-as-strings; caps/headroom config-served so clients do no authority math; metadata may carry {actorUserId, note}), scoping note (playerId from the principal, never input), failure codes (UNAUTHORIZED 401 anon/garbage, PLAYER_NOT_FOUND 404, VALIDATION_ERROR 400 bad query, INTERNAL_ERROR 500 missing wallet) and the GET-only note (route modules export exactly ['GET'], asserted by tests); the previously-planned /player/me/transactions row is struck through and marked "Superseded — delivered in Phase 5 as GET /api/v1/player/transactions".
- docs/ECONOMY_ARCHITECTURE.md — header now reads "Economy core: IMPLEMENTED — Phase 5"; §2 Money Types updated to implemented reality (config caps now, warehouse-derived capacity arrives with City & Buildings; GEMS capped at 1e8 instead of "∞"; storage-split note: five wallet fields vs Player.gems through the SAME ledger); §4 invariants table enforcement column updated to the real mechanics (conditional compare-and-decrement, clamped-delta ledger rows, grantResources same-tx idempotency, creditWithCap zero-apply writes nothing); §3.6 admin adjustment gained a "Phase 5 status" note (service implemented, HTTP surface = Phase 9, outbox not yet wired); NEW §7 "Implemented — Phase 5 (Resource & Economy Engine)" with 7.1 resources & storage split, 7.2 reason catalog, 7.3 caps & clamping semantics (MAX_DELTA before math, clamped delta recorded, skipped flag), 7.4 single write path (validate-ALL-then-write, typed INSUFFICIENT_<RESOURCE> details {needed, have}, runEconomyTransaction composition), 7.5 the five race-safety layers with the honest per-process scope + PG note, 7.6 idempotent grant contract (same-tx key, replay semantics, IDEMPOTENT_REPLAY cases, zero-residue crash behavior), 7.7 audited admin adjustment, 7.8 invariant table "proven by" tests. Flow diagrams (§3.1–3.5) intentionally left — they describe these exact code paths now.
- docs/DATABASE_DESIGN.md — IMPLEMENTED section: table-contract rows for resources/resource_transactions marked live since Phase 5 (resource ∈ GOLD…GEMS incl. GEMS rows though GEMS balances sit on players; reason ∈ the closed catalog; written only by the economy service); new "Economy ledger semantics (Phase 5)" paragraph (storage split through one ledger, conditional compare-and-decrement on both tables, config-cap clamping with the CLAMPED delta recorded, zero-applied credits write no row); new "Idempotency keys in use (idempotency_keys, Phase 5)" paragraph (action 'resource_grant', sha256 requestHash of {playerId, reason, ref, amounts}, responseBody, expiresAt = now + 24h, same-tx commit → zero-residue crashes, replayed:true / IDEMPOTENT_REPLAY semantics, ADJUST_RESOURCES audit row in the same tx); Verification paragraph extended with the per-run ledger invariant checks (Σdelta==balance incl. clamped credits, balance ≥ 0, no rows for fully-capped credits).
- docs/ARCHITECTURE.md — header Status line now includes Phase 5 (Resource & Economy Engine); View 3 balance-surface bullet gained config/economy.ts and a new bullet for src/lib/concurrency/ (keyed FIFO mutex beside leaf infra, DB conditional decrement as cross-process backstop); View 8 gained an "Implemented (Phase 5)" bullet naming the engine, the mutex, the GET-only surface and the note that D-lazy-tick references are unchanged (production accrual lands with City & Buildings); folder-structure comments updated (lib/concurrency/, config/economy.ts, services economy, /api/v1/player economy reads delivered Phases 3–5).
- README.md — status → "Phase 5 — Resource & Economy Engine: COMPLETE" chained with Phases 0–4; Live API line now lists all ten endpoints (health · auth ×4 · player ×5 incl. resources + transactions) + the GET /api index; test:integration description now says "auth + player system + economy engine", test:e2e lists the economy smoke suite (test counts/commands themselves unchanged); source layout gained lib/concurrency, config/economy.ts (caps · ceiling · reason catalog · idempotency TTL), services economy engine, app adapters "incl. resources + transactions", and unit/integration/e2e scopes mention economy (+mutex); Principle 4 extended to the economy surface.
- No source code, tests, schema, or package.json touched — docs/, README.md and worklog.md only. Sanity: all edited docs live outside the Prettier scope (docs/*.md ignored; README.md verified against .prettierignore); no build/test/lint runs performed for this docs-only task (the Phase 5 quality gate numbers quoted are the orchestrator's verified-by-execution results from the task brief).

Stage Summary:
- All six docs now match the Phase 5 implementation exactly: every number (caps 5e9/2.5e9/1e9/1e8, MAX_DELTA 1e15, page size 25/100, TTL 86400, tx bounds 10s/20s, idempotency key ≤128 chars, note 1–500 chars), every endpoint, every error code, the five race-safety layers and the six required test scenarios are code-grounded — nothing invented.
- Cross-doc decisions recorded: (1) the economy HTTP surface is documented as deliberately GET-only in ROADMAP/API_DESIGN/ECONOMY_ARCHITECTURE — every mutation flows through economy.service.ts inside server-owned transactions; (2) the originally planned /player/me/transactions is marked superseded by GET /api/v1/player/transactions; (3) the honest concurrency scope is stated everywhere (in-process mutex = single-node serialization; conditional decrement + unique idempotency keys remain the multi-process backstops; PostgreSQL needs SELECT … FOR UPDATE/advisory locks); (4) GEMS storage on Player with ledger reconciliation preserved is recorded in DATABASE_DESIGN and ECONOMY_ARCHITECTURE; (5) phase pointer advanced: Phase 5 COMPLETE, Phase 6 (City & Buildings) proposed next awaiting approval.
- Docs-only change-set, zero build risk; ready for the orchestrator to commit together with the Phase 5 implementation.

---
Task ID: 5
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 5 — Resource & Economy Engine (six-resource economy · ledger with reasons · caps/overflow · idempotent grants · race conditions · get-resources/history APIs · six required test scenarios)

Work Log:
- Inspected the repo before coding (worklog Phases 0–4, schema, services, routes, tests): ResourceWallet (5 resources) + ResourceTransaction (append-only ledger incl. GEMS/ENERGY values) + IdempotencyKey existed schema-ready from Phase 2; Phase 4 delivered registration/progression/patterns to reuse → NO migration needed; GEMS stays on Player (premium currency) and flows through the SAME ledger.
- Data-driven economy config (src/lib/game/config/economy.ts): ECONOMY_RESOURCES (wallet 5 + GEMS), RESOURCE_CAPS BigInt per resource (5e9 / 2.5e9×3 / 1e9 / 1e8), MAX_DELTA 1e15 single-mutation ceiling, ECONOMY_HISTORY {25, 100}, GRANT_IDEMPOTENCY_TTL_SECONDS 24h, closed LEDGER_REASONS catalog (BOOTSTRAP + the seven required: QUEST_REWARD, BUILDING_UPGRADE, UNIT_TRAINING, BATTLE_REWARD, MARKET_PURCHASE, MARKET_SALE, ADMIN_ADJUSTMENT), pure creditWithCap/debitBalance/isEconomyDelta/isPositiveAmount. Barrel updated.
- Concurrency primitive (src/lib/concurrency/mutex.ts): keyed in-process FIFO promise-chain mutex — per-key serialization, cross-key parallelism, error-isolated chains, idle-key eviction, documented non-reentrancy.
- Economy service (src/lib/game/services/economy.service.ts — THE single write path): applyResourceDeltas = validate-ALL-then-write in the caller's tx (unknown resource/reason → VALIDATION_ERROR; non-BigInt/zero/|delta|>1e15 → INVALID_AMOUNT; duplicate resource per batch rejected; first impossible debit → typed 409 INSUFFICIENT_<RESOURCE> {needed, have} with zero writes); debits persist via conditional compare-and-decrement (updateMany where field >= amount) on wallet AND player.gems = DB-level no-negative backstop; credits clamp at cap with the CLAMPED delta ledgered (Σdelta==balance invariant) and fully-capped credits writing nothing (skipped); grantResources (positive BigInts only) with optional idempotencyKey → IdempotencyKey row (action resource_grant, sha256 requestHash, responseBody) committed in the SAME tx → replay returns original result (replayed:true), key misuse → IDEMPOTENT_REPLAY 409, crash rolls back key+payout atomically; spendResources (positive costs → debits, all-or-nothing); runEconomyTransaction = withKeyLock(wallet:{id}) → withWriteRetry → tx {maxWait 10s, timeout 20s}; adminAdjustResources (audited: audit_logs ADJUST_RESOURCES before/after + note in-tx, ledger metadata {actorUserId, note}); reads: getWalletBalances (BigInt map), getWalletView (strings + caps + headroom), getTransactionHistory (keyset (createdAt,id) cursor "ISO|id", reason filter, limit clamp 1..100).
- APIs (GET-only by design — no client write surface anywhere): GET /api/v1/player/resources, GET /api/v1/player/transactions?limit&cursor&reason behind defineRoute + requirePlayer (+sliding-refresh Set-Cookie parity); Zod query validation; /api index lists both; APP_VERSION 0.6.0-phase5.
- Tests (217 total, all green): unit 139 (+20 economy-config: 6-key/cap/ceiling/reason invariants, credit/debit boundary math incl. exact-zero and cap edges, predicates, mutex FIFO/parallel/error-isolation/eviction); integration 65 (+25 tests/integration/economy over real routes+DB, isolated 9100005… range, afterAll cleanup incl. audit/idempotency): ALL SIX required scenarios — negative resource (typed 409 + zero writes, spend-to-zero then refuse, non-BigInt/zero/negative/unknown-reason/unknown-resource/duplicate-batch rejections), duplicate reward (replay pays once, payload-mismatch rejected, distinct keys independent, crash-then-retry fresh), concurrent update (12 parallel mixed ops → exact expected balance, 3-way contended debits → exactly 1 winner + 2 INSUFFICIENT, parallel GEMS credits, per-run Σdelta==balance + balance≥0 reconcile helper), overflow (clamp exactly at cap with correct appliedDelta, at-cap grant writes nothing), unauthorized update (route modules export exactly ['GET'], anon/garbage 401 matrix, per-row cross-player isolation proof, PLAYER_NOT_FOUND), transaction rollback (mixed batch with one impossible debit persists nothing incl. ledger counts, simulated crash-after-grant leaves zero ledger+idempotency residue); plus audited ADMIN_ADJUSTMENT (balances+metadata+audit row, overdraft refused, note required) and history API (newest-first, string amounts, cursor walk no skips/repeats, reason filter, 400s); e2e 13 (+3 economy-smoke over live HTTP: guard matrix, /api index, full exchange→wallet→history flow).
- Frontend: features/economy slice (types + useResourcesQuery/useTransactionsQuery, 401→null) + console "Resource & Economy Engine" card — LEDGER LIVE state with 6 wallet rows (balance/cap + progress) and the newest ledger entries (signed colored deltas + reason chips); roadmap/deliverables/footer updated to Phase 5.
- Fixed during dev: 3× Lowercase<string> casts for errors.insufficient, Prisma InputJsonValue cast for the stored grant result, literal-type pageSize, unused import flagged by lint, mobile 390px overflow of long code paths in the deliverables card (min-w-0 + [overflow-wrap:anywhere]).
- Quality gate (all green): eslint ✓ · tsc ✓ · prettier ✓ · unit 139 ✓ · integration 65 ✓ · e2e 13 ✓ · next build ✓ (all routes dynamic) · dev.log zero errors · Agent Browser: anonymous → dev-impersonate → SIGNED IN, economy card LEDGER LIVE with the real seeded wallet (Au 1,500 / 5,000,000,000 … Gm 0 / 100,000,000) and the five BOOTSTRAP ledger rows, desktop 1280px + mobile 390px screenshots verified, sticky footer intact, zero console/page errors.
- Docs synced by subagent Task 5-docs (ROADMAP contract table, API_DESIGN §2.3.1, ECONOMY_ARCHITECTURE implemented-state §7, DATABASE_DESIGN ledger/idempotency semantics, ARCHITECTURE status/views, README).

Stage Summary:
- Phase 5 exit criteria met: all six resources move exclusively through one server-side, transactional, ledger-first write path; negative balances are structurally impossible (validate-then-write + conditional decrement); overflow is impossible (BigInt + config caps with clamped-delta ledgering); every delta carries a closed-catalog reason; duplicate rewards are impossible (same-tx idempotency keys); concurrent mutations converge exactly (per-player FIFO mutex + tx + retry, with the conditional guard as multi-process backstop); the HTTP surface is read-only and principal-scoped.
- Key decisions: GEMS on Player but in the same ledger (storage split, one truth); caps/clamping recorded via the applied delta so the ledger always reconciles; zero-applied credits write no ledger row (ledger = economic truth, not attempt log); idempotency TTL 24h with same-tx commit (crash = zero residue); GET-only economy HTTP surface until subsystem phases add their own audited mutations.
- Awaiting approval for PHASE 6 (proposed: City & Buildings — upgrade queue, construction timers, warehouse capacity).

---
Task ID: 6
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 6 — City & Building System (17-building catalog · transactional server-side upgrades · construction timers with start/finish/status · double-spend prevention · city APIs · full test matrix)

Work Log:
- Inspected the repo before coding (worklog Phases 0–5, schema, services, routes, tests): Phase 5 delivered the complete economy engine; the Phase 2 `Building` model already carries `isConstructing` / `upgradeStartedAt` / `upgradeCompletesAt` / `pendingLevel` with `@@unique([cityId,type])` + `@@index([upgradeCompletesAt])` → NO migration needed; error taxonomy pre-seeded `BUILDING_QUEUE_BUSY`/`PREREQUISITE_MISSING`; starter bootstrap creates all 17 buildings at level 1.
- Building catalog (src/lib/game/config/buildings.ts): all 17 BUILDING_TYPES as data — maxLevel 15 (CASTLE 11 via the TH ≥ target+4 chain so it maxes exactly when TH hits 15), per-level upgrade costs through an INTEGER-only bps recurrence (`compoundBps` floor-multiply, no floats), duration growth bps, requirement model (Town Hall gate `max(floor, target−offset)`, player-level gate for TOWN_HALL `target−1`, cross-building gates ARMORY→BARRACKS and SPY_CENTER→SCOUT_CENTER), typed effects per level (productionPerHour GOLD 45·L / WOOD 50·L / IRON 40·L / FOOD 60·L, WAREHOUSE storage 20k+15k·(L−1), WALL defenseBps, TOWN_HALL queueSlots ×2 at TH10, training/research/equipment/scout/spy speed bps, HOSPITAL beds, CASTLE march slots, MARKET fee decay to a 200-bps floor) + pure materializers (`upgradeCostFor` · `upgradeDurationSecFor` · `upgradeRequirementsFor` · `effectsFor` · `constructionQueueSlots` · `materializeBuildingCatalog`). First upgrades sized against the starter wallet (asserted by tests).
- City service (src/lib/game/services/city.service.ts): tx-scoped cores `startBuildingUpgradeInTx`/`finishBuildingUpgradeInTx` + standalone wrappers on `runEconomyTransaction` (per-player wallet mutex). Upgrade validation order: catalog type → in-flight guard (CONSTRUCTION_IN_PROGRESS) → max level (MAX_LEVEL_REACHED) → requirements from REAL DB state inside the tx (PREREQUISITE_MISSING + details.missing[]) → queue slot count vs TH-derived slots (BUILDING_QUEUE_BUSY) → cost debit via `spendResources(reason BUILDING_UPGRADE, refType building, metadata {buildingType, fromLevel, toLevel, durationSec})` → conditional timer claim guarded on `isConstructing=false`. Finish: server clock only (CONSTRUCTION_NOT_COMPLETE + remainingSec) → conditional level application guarded on in-flight state → CONSTRUCTION_COMPLETE notification → `recalculatePlayerPower` — one transaction; retries typed CONSTRUCTION_NOT_ACTIVE (level applied at most once). Pure `evaluateRequirements` + `constructionStatus` exported for tests/views.
- Read models: `getCityView` (17 buildings in catalog order, derived status IDLE·CONSTRUCTING·COMPLETABLE, effects of the CURRENT level, nextUpgrade preview with server-evaluated requirementsMet + human-readable unmetRequirements, aggregate production sums, warehouse storage, queue activeCount/slots) and `getBuildingCatalogView` (fully materialized 17×levels catalog with costs as display strings — BigInt policy).
- API surface: GET /api/v1/city · GET /api/v1/city/buildings · POST /api/v1/city/buildings/[type]/upgrade · POST /api/v1/city/buildings/[type]/finish (all requirePlayer + sliding-refresh cookie parity); `defineRoute` extended ADDITIVELY with Zod-validated dynamic `params` (Next 16 Promise params; existing routes untouched); five new typed error codes (BUILDING_NOT_FOUND 404 · MAX_LEVEL_REACHED 409 · CONSTRUCTION_IN_PROGRESS 409 · CONSTRUCTION_NOT_COMPLETE 409 · CONSTRUCTION_NOT_ACTIVE 409); /api index lists all four; APP_VERSION 0.7.0-phase6.
- Frontend: features/city slice (DTO-mirror types + BUILDING_ICONS · useCityQuery with refetchInterval 3s ONLY while constructions are in flight · useBuildingCatalogQuery cached 60s · useUpgradeBuildingMutation/useFinishBuildingMutation invalidating city+wallet+ledger+player surfaces · 401→null parity) + console "City & Building System" card (capital/production-per-hour/warehouse-storage summary, per-building rows with live countdown tick, CONSTRUCTING/READY badges, cost+duration+requirement preview with the FIRST unmet reason, UPGRADE/FINISH buttons disabled unless server says requirementsMet, inline typed error surfacing incl. details.missing list, max-h-96 scroll); roadmap/deliverables/footer → Phase 6, header badge PHASE 6 COMPLETE.
- Tests (+54, all green): unit 165 (26 new: 22 buildings-config invariants + 4 defineRoute params tests); integration 90 (25 new city suite over real routes+DB, isolated 9100007… range with afterAll cascade cleanup) covering EVERY required scenario — upgrade flow (exact catalog debit, per-resource ledger rows with building ref + metadata, timer within ±2s of config duration, Σdelta==balance reconcile) · insufficient funds zero-writes · prerequisite details · max level · unknown type 404 · construction lifecycle (early claim NOT_COMPLETE, backdated claim applies level + exact power delta + notification, retry NOT_ACTIVE, queue freed) · double-spending (8 parallel same-building upgrades → exactly 1 success + exactly-once debit + 1 GOLD ledger row; 2-building queue race → 1 winner + BUILDING_QUEUE_BUSY) · rollback (crash mid-start AND mid-finish leave cost/timer/ledger/power/notification untouched via the tx-scoped cores) · unauthorized (401 matrix + exact verb exports ×4 + cross-player isolation) · catalog/city view contracts; e2e 16 (3 new: 401 guard matrix on all four endpoints, /api index entries, deterministic positive construction cycle on a FRESH per-run identity — in-flight claim helper polls the server clock, then upgrades whatever building the server flags requirementsMet, so repeated runs stay green forever).
- Fixed during dev: catalog costs initially crossed as numbers → stringified in the view (BigInt policy); test-side ledger counts corrected (one row PER debited resource is the ledger contract); GOLD_MINE/IRON_MINE fixtures swapped to TH-gate-1 buildings; castle requirement formula corrected to TH ≥ L+4; anonymous POST 401 test now sends valid params (validation precedes auth inside defineRoute by design); unused-var lint cleanup; city query now polls at 3s while a construction is in flight so the UI flips to READY without a reload.
- Quality gate (all green): eslint ✓ · tsc --noEmit ✓ · prettier ✓ · unit 165 ✓ · integration 90 ✓ (3× consecutive stable runs) · e2e 16 ✓ 0 fail on a clean run · dev.log contains ONLY expected typed-refusal warns (401 guard matrix, intentional CONSTRUCTION_NOT_COMPLETE e2e probe) — zero unhandled errors · `bun run build` intentionally NOT run (sandbox rule: it would clobber the running dev server's .next; compile validity proven by tsc strict + every route exercised live over HTTP by the e2e suite — production build deferred to the deployment phase) · Agent Browser verified the full golden path: anonymous render → dev-impersonate sign-in → city card LIVE (17 buildings, TH-gated buttons, Au 45·Wd 50·Ir 40·Fd 60 production, 20,000 storage) → Town Hall UPGRADE (wallet 1500→1100 Au / 800→550 Wd, queue 1/1, BUILDING_UPGRADE ledger pair −400/−250, countdown) → READY lv2 badge (3s poll flip) → FINISH → Town Hall lv2/15 + power 4,010 (bootstrap +200 TH weight) + "Town Hall upgraded" notification + 0/1 queue · desktop 1280px + mobile 390px screenshots · sticky footer intact · zero console errors.
- Docs synced in-repo: ROADMAP Phase 6 contract table (6.1–6.7 + city invariants + deferred cancel/collect), API_DESIGN §2.4 rewritten as IMPLEMENTED with refusal taxonomy and defineRoute params note, ARCHITECTURE status line, DATABASE_DESIGN "Construction state lives on buildings" paragraph, README status/live-API/test descriptions.

Stage Summary:
- Phase 6 exit criteria met: all 17 buildings carry level · per-level cost · duration · requirements · effects as data; upgrades are fully server-side with resources, requirements, queue and level checked inside one per-player-serialized transaction that debits through the ledger and claims the construction timer atomically; double-spending is structurally impossible (mutex → validate-then-write → conditional decrement → conditional claim, each layer test-proven); the construction system records start time / finish time / status with the server clock as the only timing authority and at-most-once completion.
- Key decisions: construction state stays on the Phase 2 `buildings` row (no migration; status derived, never stored); costs flow exclusively through the Phase 5 economy service (ledger reason BUILDING_UPGRADE, building-row ref, from/to metadata); defineRoute gained dynamic params additively (documented, backward compatible); cancel + collect deliberately deferred (outside the user's Phase 6 contract) and recorded in ROADMAP/API_DESIGN; the finish claim recalculates power from real state so the level-up effect is live the moment it lands.
- The console now has its FIRST client-write surface (upgrade/finish mutations) — still zero authority: every amount, timer and decision is server-owned.
- Awaiting approval for PHASE 7 — Army & Training. NO Phase 7 work started.

---
Task ID: 7
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 7 — Army & Unit System (11-unit roster · data-driven counters · FIFO recruitment queue · server-side training with resource validation · completion & cancellation rules · full test matrix)

Work Log:
- Inspected the repo before coding (worklog Phases 0–6, schema, economy/city services, routes, tests): Phase 5 economy exposes `spendResources`/`grantResources` + `runEconomyTransaction` (per-player wallet mutex) with `UNIT_TRAINING` already in the ledger catalog; Phase 2 baseline had `Unit`/`PlayerUnit`/`TrainingQueueItem` tables and a 6-unit placeholder roster (militia·archer·scout·swordsman·light_cavalry·catapult) with NO training service — the Phase 7 roster supersedes it.
- Unit roster (src/lib/game/config/units.ts REWRITTEN): the exact 11-unit Phase 7 contract — Infantry Swordsman/Shield Guard/Heavy Infantry (BARRACKS), Ranged Archer/Crossbowman (ARCHER_CAMP), Cavalry Cavalry/Heavy Cavalry/Knight (STABLE), Siege Catapult/Cannon/Siege Engine (ARMORY); per unit attack·defense·health·speed·foodUpkeep·carryCapacity·trainingCost·trainingTimeSec as data; T1→lv1/T2→lv5/T3→lv10 building gates via new `trainingBuilding`+`requiredBuildingLevel`; counters DATA-DRIVEN per unit at uniform 2500 bps — Infantry ▸ Cavalry ▸ Ranged ▸ Infantry with symmetric strongAgainst/weakAgainst inverses, siege with no field counters and weak vs all cavalry.
- Training policy (NEW src/lib/game/config/army.ts): ARMY_TRAINING {queueSlots 5 · maxUnitsPerBatch 1000 · notStartedRefundBps 10_000 · inProgressRefundBps 5_000 · bpsDenominator} — the ONLY place recruitment policy numbers live.
- Schema + migration `20260829223544_army_unit_training_system`: `units.trainingBuilding` + `units.requiredBuildingLevel` columns (hand-edited SQL backfills the mapping for the 6 pre-existing rows via CASE), `@@index([playerId, status])` on training_queue_items; seed pipeline extended (writes the new columns + weakAgainst + isActive, SOFT-RETIRES roster outsiders — militia/scout/light_cavalry → isActive=false, never hard-deleted because of Restrict FKs); STARTER_UNITS → 20 swordsmen + 10 archers; ARMORY effects gain `trainingSpeedBps` (siege camp) with description updated.
- Army service (NEW src/lib/game/services/army.service.ts): `recruitUnitsInTx` (unit from DB catalog — unknown AND retired ids → UNIT_NOT_FOUND 404 · count policy int 1…1000 · training building gate from REAL level → PREREQUISITE_MISSING · FIFO depth → TRAINING_QUEUE_FULL · window locked at enqueue: startedAt=max(now, tail.completesAt), completesAt=startedAt+floor(timeSec·1000·10⁴/speedBps)·count at the building's REAL speed · batch cost = catalog cost×count debited via spendResources(reason UNIT_TRAINING, refType training_queue, metadata {unitId, count, perUnitMs, speedBps, queuePosition}) — ONE transaction, any refusal zero writes) · `completeTrainingInTx` (server clock only → TRAINING_NOT_COMPLETE+remainingSec · conditional claim guarded on status=TRAINING → PlayerUnit upsert-INCREMENT (the ONLY count mutation path; no client write path exists) → power recalculated → TRAINING_COMPLETE notification) · `cancelTrainingInTx` (policy refund floor(cost×refundBps/10⁴) via grantResources(kind cancel_refund) · conditional cancel · FIFO re-walk: started windows immutable, queued followers anchor as early as `now` keeping their PAID per-unit windows) · `getArmyView` (full active roster joined with real stacks + totals unitCount/upkeepFood/carryCapacity + live queue with derived TRAINING/COMPLETABLE + per-camp speedBps) · `getArmyCatalogView` (pure config projection, costs as display strings).
- API: GET /api/v1/army · GET /api/v1/army/catalog · POST /api/v1/army/train {unitId,count} · POST /api/v1/army/train/[id]/complete · POST /api/v1/army/train/[id]/cancel (all requirePlayer + cookie parity); 5 typed error codes added (UNIT_NOT_FOUND · TRAINING_NOT_FOUND 404 · TRAINING_QUEUE_FULL · TRAINING_NOT_COMPLETE · TRAINING_NOT_ACTIVE 409); /api index lists all five; APP_VERSION 0.8.0-phase7.
- Frontend: features/army slice (DTO-mirror types + UNIT_CLASS_ICONS/LABELS · useArmyQuery polling 3s only while batches are in flight · useArmyCatalogQuery cached · recruit/complete/cancel mutations invalidating army+city+wallet+ledger+player) + console "Army & Unit System" card (standing-army/upkeep/training-speed summary, unit combobox + batch input + RECRUIT, live FIFO queue rows with countdown/CLAIM/CANCEL, full 11-unit roster list with stats + gate/cost preview, typed error surfacing); roadmap 07 COMPLETE / 08? Battle Engine NEXT, deliverables card → Phase 7, footer → awaiting Phase 8.
- Tests (+58, all green): unit 181 (16 new army-config invariants: exact contract roster, class camp mapping, tier-gate ladder, counter symmetry + triangle + siege rules, cost ceilings, refund-matrix consistency); integration 115 (25 new army suite over real routes+DB, isolated 9100009… range) covering EVERY required scenario — recruit flow (exact BigInt debits + UNIT_TRAINING ledger rows with queue ref + window math 44s/15s at base speed + counts untouched until claim) · insufficient resources (typed INSUFFICIENT_IRON 409 with needed/have, zero writes) · queue (FIFO chain anchor = tail completesAt, depth 5 then TRAINING_QUEUE_FULL with slots detail) · completion (early claim NOT_COMPLETE+remainingSec, backdated claim lands units 20→22 + DONE + exact power delta 2×136 + notification, double claim NOT_ACTIVE) · concurrent recruitment (3 parallel recruits serialize behind the wallet mutex, exact sums, monotonic chain, no negative balances) · invalid unit (unknown + retired → 404) · negative/zero/fractional/over-ceiling quantity → 400 · building gate (Shield Guard at Barracks 1 → PREREQUISITE_MISSING) · cancellation (100% queued refund exact to the cent, 50% in-progress via route, ledger refund rows with queue ref, re-walk anchors the follower exactly on the head's completesAt 22s earlier with the paid window preserved, double cancel NOT_ACTIVE, unknown id NOT_FOUND) · unauthorized (401 matrix on all five, exact verb exports ×5, cross-player TRAINING_NOT_FOUND isolation both directions) · Σledger==wallet reconcile; e2e 19 (3 new army: 401 matrix, /api index entries, full live recruit→15s poll→claim→stackCount 11→double-claim 409 cycle on a fresh per-run identity).
- Pinned baselines updated for the roster change (transparent, config-driven): power.test.ts (swordsman 136/unit; starter 3350 army + 1680 buildings = 5030), player-system.test.ts (starter army 20 swordsmen + 10 archers, power 5030/6390 assertions), quests.ts main-02 objective militia→swordsman, ROADMAP starter-power baseline note, API_DESIGN §2.5 rewritten as IMPLEMENTED (5 endpoints + refusal taxonomy + batch semantics + counter matrix).
- Fixed during dev: stale Prisma client in the long-lived dev server returned undefined trainingBuilding → 500 on live train (integration suites were green because they run a fresh process) → dev server restarted with the regenerated client (setsid double-fork so it persists across tool sessions); `getWalletBalances` returns BigInt → test comparisons switched to BigInt math; re-walk test assumed an empty queue but a prior test's head was still active → assertions now model the real FIFO; archer iron-cost slip in the concurrency sums; unused-var lint cleanup; `unitCount` in the completion payload renamed `stackCount` (unambiguous per-unit stack total).
- Quality gate (all green): eslint ✓ · tsc --noEmit ✓ · prettier ✓ · unit 181 ✓ · integration 115 ✓ · e2e 19 ✓ (18 pass + the 3 pre-existing auth/player/api smoke tests skip on their own probes — unchanged from before this phase) · `bun run build` intentionally NOT run (sandbox rule — same decision as Phase 6: compile validity proven by tsc strict + every route exercised live over HTTP by e2e/browser) · dev.log zero unhandled errors · Agent Browser verified the full golden path signed in as the allowlisted fresh identity 9100000001 (first-login bootstrap → Phase 7 starter army): console renders 17-building city + Army card (30 UNITS · 0/5 QUEUE, upkeep 50 Fd, 4×10,000 bps camps) → RECRUIT 3× Swordsman (ledger +3 UNIT_TRAINING debits, header 30 UNITS · 1/5 QUEUE, queue row live) → CANCEL (queue idle again, ledger shows the 3 refund credits — 6 UNIT_TRAINING rows total) → 11-unit combobox + roster rows verified · desktop 1440px + mobile 375px screenshots · footer pushed naturally below long content · zero console/page errors.
- Docs synced in-repo: API_DESIGN §2.5 (implemented contract), ROADMAP baseline note (Phase 7 roster supersession documented).

Stage Summary:
- Phase 7 exit criteria met: all 11 contract units carry attack · defense · health · speed · food consumption · training cost · training time as data; counter relationships are data-driven per unit (symmetric 2500-bps matrix, siege fears cavalry); the recruitment queue is a server-owned FIFO with start time / finish time / status; training enforces resource validation → building gate → queue depth → atomic ledger debit → queue append in ONE per-player-serialized transaction; completion answers to the server clock with at-most-once claims; cancellation refunds by the config policy matrix and re-walks the queue; the client can NEVER change a unit count — there is no write path outside the training lifecycle.
- Key decisions: roster replacement is total (militia/scout/light_cavalry soft-retired via isActive, never hard-deleted — Restrict FKs preserved, existing stacks untouched); training speed comes from the training building's REAL level at enqueue and the window is then immutable (paid durations, cancel re-walk preserves per-unit windows); batch semantics (units land at claim) mirror the Phase 6 construction lifecycle for architectural consistency; siege trains at ARMORY whose effects gained trainingSpeedBps; refunds cross the SAME UNIT_TRAINING ledger reason with kind=cancel_refund metadata so the ledger stays one closed vocabulary.
- Starter power baseline is now 5030 (3350 army + 1680 buildings) — unit + integration tests pin it; ROADMAP documents the supersession.
- Awaiting approval for PHASE 8 — Battle Engine. NO Phase 8 work started.

---
Task ID: 20
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 20 — Seasonal System (Season lifecycle with start/end/status/rules/rewards · seasonal ranking · exact transactional reset · permanent progression preserved · reset simulated in test environment before real execution)

Work Log:
- Inspected the repo before coding (worklog Phases 0–7, schema, economy/city/army services, auth guard, routes, tests). Phase 2 baseline already carried schema-ready `Season`/`Leaderboard` tables; no season service existed.
- Schema + migration `20260831101851_seasonal_system`: `Season.settledAt` (the at-most-once settlement claim) · NEW `SeasonRewardClaim` (unique seasonId+playerId; rank/score/tierName/rewards JSON; claimedAt conditional) · NEW `SeasonWallet` (season-scoped shards currency, unique seasonId+playerId) · NEW `Title`/`PlayerTitle` + `Cosmetic`/`PlayerCosmetic` permanent-progression catalogs + ownership rows (`@@unique` per pair, `acquiredSeason` provenance) · `Player.activeTitleId` (SetNull FK) · `Commander.isSeasonal` (the "certain commanders" contract: seasonal ones are wiped, permanent survive) · `@@index([seasonPoints])` on players. `prisma validate` ✓, migrate ✓.
- Config (NEW src/lib/game/config/seasons.ts, ~270 lines — the ONLY tuning surface): `SEASON_RULES` (durationDays 30 · namePattern · score {buildingLevelUpPointsPerNewLevel 10 · unitTrainedPointsPerTier [2,5,10]} · contiguous reward tiers 1–3 Warlord Elite / 4–10 Frontline Commander / 11–50 Seasoned Warrior with resource payouts + title/cosmetic/achievement grants · tieBreak EARLIER_JOIN · minScoreToRank) · `SEASONAL_RESET_CATALOG` (PLAYER_SEASON_POINTS · TERRITORY_OWNERSHIP · SEASON_WALLETS · SEASONAL_COMMANDERS) vs `PERMANENT_PROGRESSION_CATALOG` (ACHIEVEMENTS · COSMETICS · TITLES · PERMANENT_COMMANDERS · WALLETS · BUILDINGS · UNITS · POWER_LEVEL_XP) · TITLES/COSMETICS catalogs · `validateSeasonRules()` invariant validator (ladder contiguity from rank 1, positive-integer rewards over real economy resources, catalog references, points tables) with teeth — failing configs can never reach the DB (memoized `seasonRulesOrThrow()` in the service + unit tests).
- Settlement service (NEW season-settlement.service.ts — the transactional reset): `executeSettlementInTx` runs the FULL reset inside the CALLER's transaction — (0) server-clock state resolution then load latest season, refuses SETTLEMENT_NOT_PENDING (not ended) / SEASON_ALREADY_SETTLED; (1) AT-MOST-ONCE claim via conditional `updateMany({id, settledAt: null} → {settledAt, FINISHED})` count===1 — concurrent executors converge on exactly one winner, losers roll back entirely; (2) final ranking from REAL seasonPoints (desc, id asc tie-break) with minScoreToRank gate; (3) immutable history materialized into Leaderboard rows (category SEASON, period SEASONAL, BigInt scores); (4) per-tier `SeasonRewardClaim` rows (payouts claimed idempotently later) + REWARD notifications; permanent grants (titles/cosmetics/achievements) are pre-filtered against already-owned pairs so re-settlement can never duplicate; (5) THE WIPE walks the data-driven SEASONAL_RESET_CATALOG (seasonPoints→0 · territory ownership+lastCapturedAt stripped · season wallets deleted · seasonal commanders deleted) — permanent tables never touched; (6) next season created in the SAME transaction with the rules snapshot; (7) audit log SEASON_SETTLE (real runs only). `simulateSeasonSettlement()` = the IDENTICAL code path inside a transaction that ALWAYS rolls back (rollback-marker exception carries the report) — the required test-environment simulation: operators run simulate → review report → execute. `settleSeason(actor)` = the real reset, admin-actor required.
- Season service (NEW season.service.ts, ~695 lines): lazy scheduler `resolveSeasonStateInTx` (UPCOMING→ACTIVE at startsAt, ACTIVE→FINISHED at endsAt, resolved against the SERVER clock on every read/mutation — no cron needed) · `ensureActiveSeasonInTx` auto-renewal GUARD (only when latest season is settled or none exists — ended-but-unsettled blocks renewal so the reset can never be silently skipped) · `awardSeasonPointsInTx` (called inside the REAL action's tx from city + army services; points from config helpers; zero writes outside an ACTIVE season) · `getSeasonView` (status/rules/tier previews + me {points, deterministic live rank, shards} + settlementPending) · `getSeasonRankingView` (LIVE ranking computed server-side for the running season — points desc, id asc; history via materialized Leaderboard rows only for settled seasons, `?seasonId=` selector, SEASON_NOT_SETTLED refusal otherwise; limit 1–100) · `getMySeasonRewardsView` (pending/claimed payouts) · `claimSeasonReward` — IDEMPOTENT three layers deep: unique (seasonId,playerId) claim row → conditional `claimedAt IS NULL` update (count===1 arbiter) → ledger idempotency key `season_reward:{claimId}` on the grantResources payout; replays return the ORIGINAL payout with alreadyClaimed:true — double-pay structurally impossible · `getSeasonProgressionView` (titles/cosmetics/achievements/commanders with seasonal-vs-permanent flags) · `equipTitle` (ownership verified against the real player_titles row — TITLE_NOT_OWNED otherwise).
- Integration (server-computed score inside the real transactions): city.service `finishBuildingUpgradeInTx` awards `seasonPointsForBuildingLevelUp(newLevel)` → response exposes seasonPoints; army.service `completeTrainingInTx` awards `seasonPointsForTrainedUnits(tier, count)` → response exposes seasonPoints. ZERO client write surface over season state anywhere.
- Auth: `requireAdmin` in guard.ts — two INDEPENDENT server-side checks (authenticated user.role SUPERADMIN + an ACTIVE admin_users row) — 403 ADMIN_REQUIRED otherwise; season settlement is never client-drivable.
- Seed: TITLES + COSMETICS upserted idempotently; achievements config gained ach-season-champion/ach-season-veteran (already seeded via ACHIEVEMENTS loop).
- API (8 routes, all defineRoute + requirePlayer/requireAdmin + Zod + envelope + sliding-session cookie parity): GET /api/v1/season (status+rules+me) · GET /api/v1/season/ranking (query limit 1–100 default 10, optional seasonId history) · GET /api/v1/season/rewards · POST /api/v1/season/rewards/claim {seasonId} — body carries ONLY the seasonId, amounts/rank/tier are server-owned · GET /api/v1/season/progression · POST /api/v1/season/progression/title {titleId|null} · POST /api/v1/admin/season/settle/simulate (ADMIN) · POST /api/v1/admin/season/settle/execute {seasonNumber} (ADMIN + operator must confirm the season number — belt-and-braces 409 on mismatch). Typed errors: SEASON_NOT_FOUND · SEASON_NOT_SETTLED · SEASON_REWARD_NOT_FOUND · SEASON_ALREADY_SETTLED · SETTLEMENT_NOT_PENDING · TITLE_NOT_OWNED · ADMIN_REQUIRED. /api index updated; APP_VERSION 0.9.0-phase20.
- Frontend: features/season slice (DTO-mirror types + queries/mutations incl. ranking 15s polling, reward claim, title equip) + console "Seasonal System" section (season header with countdown · rules + tier ladder · live top-10 ranking with tier badges · my standing · pending/claimed rewards with CLAIM · permanent progression grid · title equip).
- Tests (+16 unit, all green — suite 197): config invariants (validator teeth on every rule, contiguous ladder, positive-integer rewards over real resources, catalog cross-references, rank-1 rewarded, catalogs disjoint + exactly name the user contract, points helpers deterministic + per-tier coverage of the roster).
- Quality gate (all green, verified across two sessions): eslint ✓ · tsc --noEmit ✓ · unit 197/197 ✓ · prisma validate ✓ · migrate status "up to date" ✓ · dev server health phase 20 ✓ · dev.log clean.
- Fixed during the second session: the first session's final commit had landed with a UUID placeholder message — reviewed the ENTIRE diff (schema/migration/services/routes/config/tests/seed/guard), re-ran the full quality gate, amended the commit to the proper phase message, and appended this worklog entry. No code changes were needed during review.

Stage Summary:
- Phase 20 exit criteria met: seasons carry start/end/status/rules/rewards with the rules SNAPSHOT taken at creation (mid-season balance edits can never retroactively alter a running season); seasonal ranking is computed server-side (deterministic points-desc/id-asc) and settled seasons materialize an immutable history; the reset is EXACT and TRANSACTIONAL — at-most-once settledAt claim, ranking, reward claims, permanent grants, seasonal wipe, next season and audit share ONE transaction with rollback-on-anything; permanent progression (achievements · cosmetics · titles · permanent commanders — plus wallets/buildings/units/power) survives every reset while seasonal progression (season points · territory ownership · season wallets · seasonal commanders) is wiped; the reset was SIMULATED-first by design (identical code path in an always-rolled-back transaction, surfaced to operators via the admin simulate endpoint) and reward payouts are idempotent three layers deep.
- Key decisions: lazy scheduler over cron (state resolved against the server clock on every season touchpoint — consistent with the timed-system pattern); ended-but-unsettled seasons BLOCK auto-renewal so the reset cannot be silently skipped; the wipe is data-driven (SEASONAL_RESET_CATALOG) so future phases register seasonal systems by extending config, never by editing the reset transaction; season score accrues ONLY inside the real game actions' transactions.
- Awaiting approval for PHASE 21 — Admin Panel. NO Phase 21 work started.

---
Task ID: 21
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 21 — Admin Panel (player search · details · ban/unban · resource adjustment · battle/economy inspection · event management · clan management · announcements · audit logs · RBAC with ADMIN/MODERATOR roles · every action audited · server-side authorization)

Work Log:
- Inspected the repo before coding (worklog through Phase 20, schema AdminUser/AuditLog/Announcement/GameEvent/Clan/Battle models, session service ban enforcement, Phase 5 adminAdjustResources, ADMIN_ARCHITECTURE.md design, page.tsx console structure, integration test conventions).
- RBAC config (NEW src/lib/game/config/admin.ts — the ONLY authorization surface): ADMIN_SCOPES catalog (17 scopes across players/inspections/events/clans/announcements/governance) · ADMIN_ROLE_SCOPES matrix (MODERATOR: search/view/ban/unban + read-only inspections + announcements.create + audit.view; ADMIN: everything; legacy seeded SUPERADMIN resolves with the full ADMIN set via normalizeAdminRole) · adminRoleHasScope/adminScopesForRole resolvers (fail-closed on unknown roles) · typed confirmation phrases (clanDisband 'DISBAND' · seasonSettle 'RESET SEASON') · ADMIN_PANEL_POLICY numbers (page sizes, reason bounds, tail rows, flow window, broadcast cap) · validateAdminRbac() with STRUCTURAL moderator denials (money/power scopes can never be granted to moderators — validator + unit tests).
- Guard (src/lib/auth/guard.ts REWRITTEN): requireAdminScope(request, scope) — authenticate → load ACTIVE admin_users row (the single DB-backed authorization; none → 403 ADMIN_REQUIRED) → resolve scope through the matrix (missing → 403 ADMIN_FORBIDDEN). Removed the old requireAdmin (User.role==='SUPERADMIN' gate) — the AdminUser row is now the ONLY source of truth; the two Phase 20 settle routes migrated to requireAdminScope('season.settle'). resolveAdminContext for GET /admin/me (UI introspection — explicitly NOT an enforcement surface).
- Ban enforcement is NOT new code — it was already structural: session.service assertNotBanned runs on EVERY authenticated request (DB is the per-request authority), so an admin ban takes effect on the victim's very next request, panel included.
- Migration 20260831114049_admin_panel_rbac: events.title/body/createdById columns + players @@index([name]) for search. Prisma validate ✓ migrate ✓.
- Admin services (NEW src/lib/game/services/admin/ — 8 modules + barrel):
  - admin-audit.service: recordAdminAuditInTx (mutations write their audit INSIDE the same tx — before/after snapshots; an untracked mutation is structurally impossible) · recordAdminAuditView (READ inspections audited best-effort — privacy trail, never breaks the read) · listAuditLogs viewer (action/targetType/actor filters, paging) · shared resolvePaging.
  - admin-players.service: searchPlayers (name contains / exact id / exact telegramId / username, ban state on every row, audited) · getPlayerDetails (identity + ban state + six-resource wallet + city w/ town hall + army totals + ledger tail 12 + battles tail 6, audited) · banPlayer (reason REQUIRED 4…300, optional temp-ban expiresAt, conditional updateMany isBanned:false→true count===1, PLAYER_ALREADY_BANNED 409, audit BAN with before/after in-tx) · unbanPlayer (conditional inverse, PLAYER_NOT_BANNED 409, audit UNBAN).
  - Resource adjustment REUSES the Phase 5 adminAdjustResources — ledger reason ADMIN_ADJUSTMENT, no-negative/no-overflow invariants, audit row with before/after balance snapshots in the SAME transaction; the route adds the RBAC gate (ADMIN-only players.adjust_resources) and Zod ({resource ∈ economy enum, signed non-zero int delta, note 4…500}).
  - admin-battles.service: list (filters playerId/type, paging) + detail (full stored trace: seed, configVersion, powers, loot, ordered rounds with events, per-participant reports) — read-only, audited; honest empty state until the Battle Engine lands.
  - admin-economy.service: supply per resource from REAL wallets (+player.gems), ledger flow by reason over the 30d policy window (count/net/mint/burn), recent audited adjustments — all server-side aggregates.
  - admin-events.service: list (status filter) · createEvent (typed whitelist of 7 event types, window validation endsAt>startsAt, PLAYER scope demands an existing target, status derived from the clock, creator recorded) · finishEvent/cancelEvent (conditional status claims SCHEDULED|ACTIVE→FINISHED/CANCELLED count===1 — concurrent operators converge, double-finish EVENT_NOT_ACTIVE).
  - admin-clans.service: list (name/tag search, trophies order) · detail (full member roster) · disbandClan (typed 'DISBAND' confirmation validated SERVER-side, reason required, war history REFUSED CLAN_HAS_WARS — Restrict ledger never orphaned, pre-deletion snapshot audited, dependents deleted + denormalized player.clanId/clanRole cleared in ONE tx).
  - admin-announcements.service: list · create (title 4…120, body 4…2000, audience ALL/CLAN/PLAYER w/ clan existence check) · setActive (conditional toggle, audited) · broadcast (active-only ANNOUNCEMENT_INACTIVE, audience-resolved recipient fan-out via notification.createMany in ONE tx, count on the audit row).
  - admin-staff.service: listStaff · grantStaff (by telegram id, ADMIN/MODERATOR upsert — idempotent re-role, same-role no-op refused, USER_NOT_FOUND for unregistered ids, audit STAFF_GRANT/STAFF_ROLE_SET) · deactivateStaff (conditional isActive claim, SELF_TARGET lockout guard, audit STAFF_DEACTIVATE) — grants/revokes take effect on the staff member's NEXT request because the guard re-reads the row per request.
- API (21 routes under /api/v1/admin, ALL via defineRoute + Zod + envelope + session cookie parity, EVERY route declares its scope server-side): me · players (GET) · players/:id (GET) · players/:id/ban|unban|resources (POST) · battles (GET) · battles/:id (GET) · economy (GET) · events (GET view + POST manage) · events/:id/finish|cancel · clans (GET) · clans/:id (GET) · clans/:id/disband · announcements (GET view + POST create) · announcements/:id/active|broadcast · audit-logs (GET) · staff (GET+POST manage) · staff/:id/deactivate. Typed errors added: ADMIN_FORBIDDEN · PLAYER_ALREADY_BANNED · PLAYER_NOT_BANNED · EVENT_NOT_ACTIVE · EVENT_WINDOW_INVALID · CLAN_HAS_WARS · CONFIRMATION_REQUIRED · ANNOUNCEMENT_INACTIVE · USER_NOT_FOUND · STAFF_NOT_FOUND · EVENT_NOT_FOUND · ANNOUNCEMENT_NOT_FOUND. /api index lists the full surface; APP_VERSION 0.10.0-phase21.
- Frontend: features/admin slice (DTO-mirror types + api/admin.ts hooks — 403 surfaced as a typed error state because RBAC denial is a meaningful UI condition) + components/admin-panel.tsx: the panel renders ONLY for DB-backed staff sessions (visibility = UX, never the boundary), 8 sub-tabs driven by the server-issued scope list (Players search+details dialog with ban/unban/adjust forms · Battles list+trace dialog · Economy supply/flow/adjustments · Events filter+spawn+finish/cancel · Clans search+roster+typed-confirmation disband · Announcements create/toggle/broadcast · Audit filter+trail · Staff grant/revoke), pagination everywhere, destructive actions gated client-side by phrase/note length (server re-validates). Console page: AdminPanel section inserted (single-route rule), roadmap + deliverables updated to Phase 21.
- Tests (+21 unit, +31 integration, all green): unit admin-rbac (strict MODERATOR⊂ADMIN hierarchy, structural money/power denials, contract scope presence, fail-closed unknown roles, legacy SUPERADMIN resolution, confirmation phrases, policy sanity); integration admin-system over real routes+DB (isolated 9100021… range): RBAC matrix (anonymous 401 · player ADMIN_REQUIRED · moderator in-scope 200 + out-of-scope ADMIN_FORBIDDEN ×3 · /admin/me role+scopes) · ban flow (blocks EVERY route incl. admin panel with BANNED 403, double-ban 409, unban restores, non-banned unban 409, BAN/UNBAN audit rows with before/after+reason) · adjustment (ledger credit exact, audit after.GOLD exact, overdraft typed INSUFFICIENT_GOLD 409 with zero writes) · search by name+telegramId, details aggregates + VIEW audit row, economy aggregates incl. ADMIN_ADJUSTMENT flow · events (spawn/status, past window 400, finish → double-finish EVENT_NOT_ACTIVE, unknown 404) · clans (wrong phrase 400 intact, DISBAND clears members+denormalized fields+audit snapshot) · announcements (moderator create, broadcast fan-out == player count + notification row + audit count, deactivate → broadcast ANNOUNCEMENT_INACTIVE, toggle audited) · staff lifecycle (grant moderator by telegram id → acts in scope immediately, out-of-scope 403, same-role re-grant 400, self-deactivate SELF_TARGET, revoke → next request ADMIN_REQUIRED) · audit viewer (all phase actions present, action filter).
- Quality gate (all green): eslint ✓ · tsc --noEmit ✓ · prettier ✓ · unit 207/207 ✓ · integration 146/146 ✓ (31 new admin) · prisma validate + migrate ✓ · dev.log clean.
- Agent Browser verified end-to-end as the seeded admin: panel renders only for staff (anonymous session → ZERO admin tablist elements; the roadmap text match was page copy) · 8 tabs · player search found the seeded pair · details dialog (city TH1 · 17 buildings · ban ACTIVE · ledger tail) · RESOURCE ADJUSTMENT from the UI (+1000 GOLD → wallet 2500, audit row after.GOLD=2500 exact) · Economy tab supply aggregates EXACT (3×1500 base +1000 adjust = 5500 GOLD) with flow-by-reason + recent adjustments · Announcements created + broadcast ("3 player(s) notified") · Audit trail lists every action INCLUDING read inspections (16 rows) · Staff tab with grant form + seeded SUPERADMIN row · desktop 1440px + mobile 390px full-page renders · zero console/page errors · replay defense observed live (a second impersonation rotated the session — old token correctly dead with "Session is not recognized", re-login restored).
- Environment note: dev-only secrets (JWT_SECRET · TELEGRAM_BOT_TOKEN · ADMIN_SECRET · ADMIN_TELEGRAM_IDS) added to the UNTRACKED .env to run integration suites + browser verification (previous sessions evidently had them; the sandbox .env had been reset to DATABASE_URL-only). No secrets committed.

Stage Summary:
- Phase 21 exit criteria met: the admin surface covers Player Search · Player Details · Ban · Unban · Resource Adjustment · Battle Inspection · Economy Inspection · Event Management · Clan Management · Announcements · Audit Logs; RBAC distinguishes ADMIN (full) from MODERATOR (moderation + read-only inspection + announcements) with the matrix in config and structural denials for the money/power scopes; EVERY admin action is audited (mutations transactionally with before/after, inspections best-effort VIEW rows); the Admin API is authorized server-side on every request by the DB-backed admin_users row + scope matrix — no route relies on a hidden frontend button; destructive ops (disband) demand a typed confirmation phrase validated server-side; bans are enforced by the session layer on every request, so they bind instantly.
- Key decisions: AdminUser row = single authorization truth (the legacy SUPERADMIN User.role gate replaced by scope resolution; season settle migrated to the season.settle scope); resource adjustment rides the Phase 5 ledger path so every coin stays inside the ledger's closed vocabulary; announcements broadcast fans out through the existing notification inbox; clan disband refuses clans with war history rather than orphaning Restrict-protected rows; the panel hides UI for missing scopes purely as UX — enforcement never leaves requireAdminScope.
- Awaiting approval for PHASE 22. NO further-phase work started.

---
Task ID: 22
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 22 — Notification System (engine · 10 event types · queueable · duplicate prevention · Telegram-ready · worker architecture)

Work Log:
- Inspected the repo before coding (worklog through Phase 21, schema Notification/GameEvent/Leaderboard/Player models, all six DIRECT tx.notification.create producers — bootstrap welcome, progression level-up, city finish, army claim, settlement REWARD, announcement broadcast — the Phase 21 RBAC scope catalog + requireAdminScope guard, env config layer, route factory, integration test conventions). Key reconnaissance finding: phases 8–19 were never built (no commits), so Attack/Quest/Clan/WorldBoss are schema-only systems — the engine was built complete for ALL 10 event types, with emitters wired into every transaction that exists today and payload contracts finalized for the future ones.
- Migration 20260831133614_notification_system: NEW notification_queue table (playerId · type · dedupeKey · channels Json · payload Json · status PENDING|PROCESSING|SENT|FAILED|SKIPPED · attempts/maxAttempts · availableAt · claimedAt/claimedBy · lastError · notificationId backlink) with @@unique([playerId,type,dedupeKey]) (THE dedupe constraint), @@index([status,availableAt]) for the claim scan, cascade on player. SQLite↔PG-valid (no enums, Json columns). Sandbox hiccup fixed: the first migrate dev run died mid-flight and left a garbage-named migration folder + mismatched _prisma_migrations row — folder renamed, record corrected, migrate status clean, schema/migration/diff all verified consistent.
- Notification catalog (NEW src/lib/game/config/notifications.ts — the single contract producers and the worker share): NOTIFICATION_PAYLOAD_SCHEMAS — a per-type Zod schema validated AT ENQUEUE (fail-closed: a producer cannot enqueue what the renderer cannot render), incl. a discriminated union for RANK_CHANGE's two real producers (LEVEL_UP vs SEASON_RANK); NOTIFICATION_TYPE_CHANNELS (time-critical combat/diplomacy/world types also push TELEGRAM; progression types inbox-only); server-side renderNotification templates per type; notificationDedupeKeys builders carrying EVENT IDENTITY (construction:{buildingId}:{level} · training:{queueItemId} · level_up:{player}:{level} · season_rank:{season}:{player} · event:{id} · announcement:{id} · attack_in:{march}:{defender} …) so entity reuse never re-notifies while retries never duplicate; NOTIFICATION_POLICY (batch 25 · tick 15s · maxAttempts 5 · exponential backoff 5s→5min cap · staleClaim 120s · retention 30d · bounds); notificationBackoffDelayMs pure fn; validateNotificationCatalog invariant checker.
- Telegram adapter (NEW src/lib/telegram/send-message.ts): REAL Bot API sendMessage (no stubs) — JSON POST with timeout, response parsing, and retry classification (429/5xx/network → retryable; 400/403/404 blocked-bot → permanent). Capability-honest: resolveTelegramDeliveryConfig reads env; unconfigured push → delivery SKIPPED with an explicit reason, never a fake success.
- Engine (NEW src/lib/game/services/notification.service.ts): enqueueNotificationInTx (validates payload + resolves channels ⊆ catalog, P2002 → idempotent no-op) called INSIDE game transactions; enqueueNotificationFanOutInTx (per-player dedupe keys + payloads; Prisma skipDuplicates is UNSUPPORTED on SQLite so the skip is computed by pre-reading existing (player,type,dedupeKey) pairs with a per-row P2002 fallback for racers — correct on both drivers); drainNotificationQueue (candidate scan = due PENDING + stale PROCESSING; per-row atomic claim UPDATE count===1 so N workers converge; render → IN_APP inbox create idempotent via notificationId backlink → push channels; retry = attempts++ + backoff re-arm; terminal SENT/SKIPPED/FAILED; unexpected throws never kill the tick); DI seams for tests (now · fetchImpl · telegramConfig · batchSize) — production paths untouched; pruneNotificationStorage (terminal queue rows + READ inbox rows past retention; unread persist by design); listPlayerNotifications / countUnread / markNotificationsRead (own-rows-only updateMany, ids≤100).
- Worker architecture: in-process runtime (NEW src/lib/game/worker/notification-worker.ts) started ONCE per server process via src/instrumentation.ts register() (nodejs runtime only, globalThis singleton, unref'd timers, disabled under NODE_ENV=test, boot tick 5s + interval 15s, error-proof loop) — the Mini App deployment is self-sufficient with no external cron, and the atomic claim makes horizontal scaling safe. Operators/tests get a synchronous drain: POST /api/v1/admin/notifications/worker/tick (new RBAC scope notifications.drain, ADMIN-only, added to config/admin.ts + ADMIN_PANEL matrix) and an ops view GET /api/v1/admin/notifications/queue (status/type filters, groupBy stats, recent rows). New typed routes: GET /api/v1/player/notifications?limit&unreadOnly · /unread-count · POST /read {ids|all} — all defineRoute + Zod + envelope + session parity; /api index lists the full surface; APP_VERSION 0.11.0-phase22 · APP_PHASE 22.
- Emitters migrated INTO the engine (same-transaction, dedupe-keyed): city.service CONSTRUCTION_COMPLETE · army.service TRAINING_COMPLETE · progression.service RANK_CHANGE(LEVEL_UP) · player-bootstrap REWARD welcome · season-settlement REWARD→RANK_CHANGE(SEASON_RANK) fan-out per ranked player (report field renamed notificationsCreated→rankChangesEnqueued; settlement's at-most-once claim + (season,player) key = double protection) · admin-announcements broadcast → engine fan-out (replay-proof) · NEW: admin-events createEvent now fans EVENT notices (PLAYER scope → target; GLOBAL/CLAN → capped population) with the notified count on the audit row. ATTACK_INCOMING/RESULT, QUEST_COMPLETED, CLAN_INVITE, CLAN_WAR, WORLD_BOSS are fully engine-ready (schemas+templates+channels+keys) pending their systems' phases.
- isUniqueConstraintError extracted to NEW leaf module services/prisma-errors.ts (breaking the bootstrap→engine→registration import cycle; player-registration re-exports for compatibility).
- Frontend: NEW features/notifications slice (types · api hooks — unread badge polls 30s, list on open, mark-read invalidations) + components/notification-bell.tsx (shadcn Popover/Badge/ScrollArea; amber unread count bubble, per-type labels, relative timestamps, per-row + mark-all read, honest empty state). page.tsx: bell in the header (renders ONLY for signed-in sessions — visibility is UX, the API is the boundary), roadmap + deliverables + phase badge updated to 22.
- Tests (+14 unit, +20 integration, 4 suites updated): unit notifications-config (catalog covers all 10 required event kinds + REWARD + ANNOUNCEMENT; IN_APP mandatory; TELEGRAM types pinned; fail-closed payload rejections incl. RANK_CHANGE union; deterministic renders for both rank kinds; dedupe keys distinct-per-event yet stable-per-event; backoff exponential + capped + monotonic). Integration notification-system over real routes+DB (isolated 9100033… range, injected transport — the network is NEVER touched): RBAC (anonymous 401 · player ADMIN_REQUIRED on tick/queue · admin drains + views) · dedupe (same key → 1 row; new event → separate; bad payload/unblessed channel rejected) · end-to-end (PENDING→claim→render→inbox→SENT with backlink; re-drain no-op) · telegram (configured+OK push carries rendered text to the right chat id; unconfigured → SKIPPED w/ reason + inbox intact; 429 → PENDING w/ attempts=1 + future availableAt and inbox delivered pre-failure; 403 → terminal FAILED) · recovery (stale PROCESSING re-claimed → SENT; poison row bounded to FAILED at exactly maxAttempts) · inbox API (newest-first, unreadOnly, cross-player ids cannot mark others' rows, read-all) · fan-outs (GLOBAL event reaches the whole population, PLAYER scope exactly the target, audit counted; settlement-style RANK_CHANGE fan-out replay → 0 new) · retention (terminal queue + read inbox pruned past 30d, unread survive). Updated city/army/player/admin suites to drain before asserting inbox rows. Engine bug caught BY the tests and fixed: attempts were double-counted (claim increment + manual +1) — maxAttempts semantics now exact.
- Quality gate (all green): eslint ✓ · tsc --noEmit ✓ · prettier ✓ · unit 221/221 ✓ · integration 166/166 ✓ · prisma validate + migrate status "up to date" ✓ · dev.log clean (only expected 401 warns from live-server auth tests). Dev-only secrets (JWT_SECRET · TELEGRAM_BOT_TOKEN · ADMIN_SECRET · ADMIN_TELEGRAM_IDS incl. the auth-suite impersonation id + a browser-test id) restored into the UNTRACKED .env — the sandbox reset had wiped them again; nothing secret committed.
- Agent Browser verified end-to-end: signed-in via dev impersonation → the in-process worker delivered the bootstrap welcome ~15s after login (instrumentation proven live) → bell showed "1 unread" badge → popover rendered the server-rendered welcome (type badge, relative time) → per-row mark read flipped the badge to "0 unread" → anonymous session renders NO bell → desktop 1440px + mobile 390px layouts hold → footer sticks to the viewport bottom on the long page (footerBottom == innerHeight verified geometrically) → zero console errors.

Stage Summary:
- Phase 22 exit criteria met: the Notification Engine is queueable end-to-end (durable rows, atomic multi-worker-safe claims, bounded retries with exponential backoff, stale-claim crash recovery, retention pruning); duplicate sending is structurally impossible ((playerId,type,dedupeKey) unique + event-identity keys + idempotent enqueue/fan-out); all 10 requested event types exist as first-class validated/rendered/channel-routed catalog entries; Telegram Bot integration is real and env-gated (sendMessage transport + retry classification + honest SKIPPED when unconfigured); the worker architecture is in-process via instrumentation with an admin synchronous tick and an ops view — no second deployable, horizontally scalable.
- Key decisions: ONE canonical emit surface (enqueueNotificationInTx inside the causing transaction — notifications now share the ledger's all-or-nothing discipline); dedupe keys encode EVENT IDENTITY not entity ids; engine-ready-but-unwired types documented for the phases that own their systems (attack/quest/clan/boss) — no fake call sites; inbox delivery is idempotent across worker crashes via the notificationId backlink; push failures never swallow inbox delivery.
- Awaiting approval for PHASE 23. NO further-phase work started.

---
Task ID: 23
Agent: Z.ai Code (orchestrator — senior team role)
Task: PHASE 23 — Security Audit (full-repo review as Security Engineer; fix all visible exploits; regression tests per bug; secrets scan; dependency audit; Security Report)

Work Log:
- Inspected repo first per standing rule: git log confirmed Phases 21 (admin panel) and 22 (notifications) committed and complete; enumerated the full 54-route API surface; read the auth core (guard/session/jwt/cookies/telegram init-data/route-handler/rate-limit/request-info/env) line-by-line.
- Ran three parallel audit passes with every candidate finding hand-verified in source before acceptance: (A) route wiring — all 54 routes × defineRoute usage, guard, RBAC scope fit, IDOR, schema bounds, throttles; (B) game services — transactionality, atomic claims, economy integrity, idempotency, admin services, mutex; (C) infrastructure/frontend — secrets (incl. git-history archaeology), env exposure, CORS, headers, XSS sinks, SQLi, CSRF, logging hygiene, dev-impersonation, dependencies.
- Verified-clean highlights: Telegram initData = official HMAC algorithm with constant-time compares and signature-before-parse; JWT pins HS256 + issuer/audience with DB session rows as the revocation/token-hash authority; zero NEXT_PUBLIC_ leaks; single static `SELECT 1` raw query; zero XSS sinks; no secrets ever committed (verified `git show 5958047:.env` = local SQLite path only); logger redacts sensitive keys; dev-impersonation prod-404'd, allowlisted, audited.
- FIXED [HIGH] ECON-1 minting window: economy CREDITS were absolute last-write-wins (only the in-process mutex serialized) → `persistWalletCredit`/`persistGemsCredit` bounded compare-and-set loops with cap re-clamping and exact ledger reconciliation; exhaustion = typed 409 RESOURCE_WALLET_CONFLICT.
- FIXED [MEDIUM] AUTH-1 privilege escalation: moderators could ban an active staff member's player and lock admins out of the panel (ban is enforced on every request) → banPlayer refuses active-staff targets (PROTECTED_TARGET) and self (SELF_TARGET); inactive-staff boundary pinned deliberately bannable.
- FIXED [MEDIUM] NOTIF-1 duplicate delivery: notification worker created the inbox row outside any transaction and finalized without claim ownership → ONE claim-guarded transaction for inbox+backlink (ensureInboxDelivered) and claim-identity-conditional finalize (STALE outcome); inbox exactly-once, push honestly at-least-once.
- FIXED [MEDIUM] API-1 rate limiting: clientIp took the client-forgeable FIRST XFF hop and only auth routes were throttled → last-trusted-hop extraction + data-driven principal rate-limit groups enforced in requireAuth on every authenticated route (standard 300/min backstop; playerWrite 120; adminRead 240; adminWrite 60; adminBroadcast 5; adminSettle 5; adminDrain 30) wired by intent per route.
- FIXED [MEDIUM] INFRA-1: next.config.ts now emits CSP frame-ancestors ('self' + web.telegram.org + *.telegram.org — Mini App requirement, nothing broader), nosniff, Referrer-Policy, Permissions-Policy, prod HSTS.
- FIXED [LOW] API-2 64 KiB body cap BEFORE JSON.parse (typed 413 BODY_TOO_LARGE) · API-3 foreign-Origin rejection on unsafe methods (FORBIDDEN_ORIGIN — CSRF defense-in-depth beyond SameSite=Lax) · ADMIN-1 wired the seasonSettle 'RESET SEASON' typed confirmation into the execute route · NOTIF-2 fan-out chunked at 500 rows under SQLite/PG bind ceilings · ECON-2 idempotency TTL honored on the fast-path + pruneExpiredIdempotencyKeys in the ops tick · PROG-1 grantXp compare-and-set with level re-derivation · API-4 notifications list query through the route spec (400 not 500) · ADMIN-2 admin event config bounded (keys ≤64, ≤8 KiB serialized) · SUPPLY-1 removed dead deps (next-auth with zero imports + three unused UI libs) · OPS-1 recreated .env.example (empty placeholders).
- Dependency audit: extracted all 626 resolved versions from bun.lock → npm advisory DB: 0 critical; updated next → 16.3.3 (Image-Optimizer DoS, RSC deserialization DoS, rewrite smuggling closed) + uuid/next-intl/minimatch; remaining 16 advisories are newest-version-pinned upstream backlog (build-time transitives or no reachable sink) — documented per-package exposure in the report.
- Secrets scan: repo-wide patterns + git history archaeology — clean; .env.example recreated; dev .env holds only labeled dev-only placeholders (untracked).
- Wrote SECURITY_REPORT.md: verdict matrix across every requested category, finding-by-finding evidence with fixes, verified-clean evidence section, dependency audit, honest limitations register (in-memory limiter per-process, unknown-IP shared bucket, at-least-once push, sandbox gateway XTransformPort flagged as platform infra, settlement scale note, GET /auth/me sliding refresh, upstream backlog).
- Regression tests: NEW tests/unit/security-hardening.test.ts (14: body cap, origin rejection incl. PUT/PATCH/DELETE + GET immunity, last-hop clientIp, rate-group exhaustion/isolation/standard-budget) and NEW tests/integration/security/security-hardening.test.ts (19: foreign-Origin 403 pre-auth + same-origin/native pass + 413, staff-ban rails incl. inactive-staff boundary, settle confirmation, 6th broadcast 429, unlocked CAS grants + mixed convergence with Σ(ledger)==balance, idempotency TTL re-execute + selective prune, XP CAS sum, stale-claim re-delivery keeps inbox at exactly 1, mark-read IDOR no-op, 600-row chunked fan-out deduped on replay).
- Test-infra hardening found during bring-up: admin/city/player/army suites' drains now inject a null-token telegram config (the network is NEVER touched — matches the notifications suite's own rule); the security suite self-heals its 910004… fixture ranges and cleans its bulk rows so bounded drain batches in other suites are never starved.
- Quality gate (all green): eslint ✓ · tsc --noEmit ✓ · prettier ✓ · unit 235/235 ✓ · integration 185/185 ✓ · prisma validate ✓ · dev.log clean. Frontend bumped to v0.12.0-phase23 (badge, roadmap row, deliverables card, footer).
- Agent Browser verified live: anonymous render clean → dev-impersonation session → SIGNED IN panel with bootstrapped player/wallet/city → notification bell showed the worker-delivered welcome (1 unread) → popover rendered the server-rendered body → per-row mark read flipped the badge to zero → PHASE 23 COMPLETE badge + v0.12.0-phase23 → footer gap-free at document bottom (natural push on the long page) → mobile 390px + desktop 1440px without horizontal overflow → security headers verified on live responses → zero console errors.

Stage Summary:
- Phase 23 exit criteria met: every requested category audited with evidence (auth, authorization, Telegram init data, API validation, rate limiting, CORS, secrets, SQL injection, XSS, CSRF, IDOR, privilege escalation, replay, races, duplicates, economy/battle/market/clan/admin exploits); 15 fixes landed across 9 HIGH/MEDIUM/LOW security properties; battle/market/clan exploit surfaces are pending their systems' phases and are marked N/A rather than claimed; each fix carries a dedicated regression test (33 new tests, all green).
- Key decisions: DB-level guards now mirror every in-process serialization assumption (credits CAS, XP CAS, claim-ownership conditions) so the PostgreSQL multi-instance target inherits correctness by construction; principal-keyed rate limits key on the DB-resolved identity, never on client-controllable inputs; the security report keeps an honest limitations register instead of claiming global guarantees.
- Awaiting approval for PHASE 24. NO further-phase work started.

---
Task ID: 8
Agent: Z.ai Code (orchestrator — Senior QA Engineer role)
Task: PHASE 24 — Full QA & Testing (unit / integration / API / E2E / security / regression across all 15 critical scenarios, negative testing matrix, bug lifecycle, coverage report)

Work Log:
- Full repo inspection first (per phase rules): git log confirmed Phases 0–7 + 20–23 committed (16 commits); enumerated all 55 API routes, 24 services, 54 Prisma models and the existing 33-test baseline; ran all three suites — 439/439 PASS.
- Coverage-gap analysis against the Phase 24 scenario list found: season system (Rewards/Ranking/Progression/Settlement) had ZERO integration tests; no cross-cutting negative matrix; no full-journey E2E; quests/technologies/items/achievements/starter catalogs untested. Battle/Market/Commander/Technology/Quest user-facing APIs do not exist (only models + admin management) — documented honestly as N/A instead of claiming coverage.
- NEW tests/unit/game/catalogs-config.test.ts (26 tests): quest prerequisite-graph acyclicity + reward-key domains, technology prerequisite reachability/tier ordering/positive costs, item sell-price and chest-grant integrity, achievement targets, starter-kit referential integrity (wallet keys, building coverage, roster ids, quest ids).
- NEW tests/integration/season/season-system.test.ts (30 tests): season view + rules projection; deterministic live ranking (points desc, id-asc tie-break, tier boundaries 1–3/4–10/11–50, min-score filter, limit abuse → 400); claim refusals (401/400/404/409 pre-settlement); transactional settlement end-to-end (RBAC 401/403, typed confirmation zod rail, clock authority, tier assignment 3/7/2, global points wipe, next-season creation, at-most-once replay); EXACT tier payouts through the ledger with replay = alreadyClaimed and 5-way concurrent claims converging on exactly one payout (double-pay regression); permanent progression grants + title-equip ownership (TITLE_NOT_OWNED IDOR regression) + clear; RANK_CHANGE delivered once per ranked player via the Phase 22 queue; materialized history (limit honored, unsettled → 409, unknown → 404).
- NEW tests/integration/negative/negative-matrix.test.ts (28 tests): 8-route-family anonymous matrix + garbage bearers; expired credentials (24h-old initData, wrong-secret JWT, negative-TTL JWT → SESSION_EXPIRED, DB-expired session row); invalid/oversized/injection-flavored ids with zero-write proofs; negative/zero/fractional/NaN/huge amounts (per-batch ceiling typed refusal); duplicate initData replay (no session farming) + mark-read idempotency; malformed JSON / wrong-shape bodies / >64KiB 413 / foreign-Origin 403; garbage cursors → typed 400; 8-way concurrent same-player training (exactly 5 win, 3 × TRAINING_QUEUE_FULL, money debited exactly 5× cost, wallet never negative).
- NEW tests/e2e/full-journey.test.ts (8 tests): one continuous session — register → auth/me → city roster → REAL 12s FARM L2 construction (early-claim 409, exact ledger debit, seasonPoints 20) → REAL 44s swordsman batch ×2 (early-claim 409, exact debit, roster 20→22, seasonPoints 4) → season standing 24 pts + ranking row → notification fan-out (CONSTRUCTION_COMPLETE + TRAINING_COMPLETE) → mark-read idempotency → state/statistics. Zero mocks, server clock as only timing authority.
- BUG FOUND & FIXED (product): season-settle replay reported SETTLEMENT_NOT_PENDING ("seasonNumber does not match") instead of the truthful SEASON_ALREADY_SETTLED — the route's number-match check fired before the service's at-most-once claim; fixed in src/app/api/v1/admin/season/settle/execute/route.ts (requested number settled → SEASON_ALREADY_SETTLED) and regression-locked in the season suite.
- Verified-correct behaviors documented (test expectations fixed, product untouched): building points = 10 × newLevel; training window = per-unit × batch count; drainNotificationQueue default batch 25 (worker re-ticks by design).
- Test-infra defects found & fixed during QA: request helper body stringification; stale initData age corrected to the real 24h window; identity tracking centralized with self-healing beforeAll range wipe (repeat runs stable); BigInt literal in money assertion; tie-break twin re-zeroed to keep settlement deterministic; E2E field name (notifications) + 90s explicit timeouts for real-timer steps.
- Quality gate (all green): bun run test → 261/261 · bun run test:integration → 243/243 · bun run test:e2e → 27/27 · total 531 tests / 0 failures / ~9.6k assertions · tsc --noEmit clean · eslint clean · dev server healthy (/api/health db up).
- Wrote docs/PHASE24-TEST-COVERAGE-REPORT.md: scenario coverage matrix (15 scenarios), negative-testing matrix, bug lifecycle records (reproduce/diagnose/fix/regression), honest gaps register (battle/market/commander/tech/quest user APIs pending their phases).

Stage Summary:
- Phase 24 exit criteria met: all six test layers executed, all 15 critical scenarios addressed with the 6 unimplemented ones honestly marked N/A, full negative matrix landed, one real product bug fixed with a regression test, +92 tests (439 → 531, all green).
- Key decisions: every concurrency/idempotency assertion reconciles against the LEDGER (Σ deltas == balance), never against in-memory expectations; test identities stay in isolated 9100xxxx ranges with self-healing setup so repeat runs are deterministic; drain loops mirror the production worker's re-tick semantics.
- Awaiting approval for the next phase. NO further-phase work started.

---
Task ID: 9
Agent: Z.ai Code (orchestrator — Performance Engineer role)
Task: PHASE 25 — Performance & Scalability (benchmark for ~200 concurrent players · API latency · DB queries · N+1 · memory/CPU · leaderboard · indexes · targeted caching · load-test scenarios · bottleneck fixes)

Work Log:
- Full repo inspection first: verified hot-path query patterns, the 49 schema indexes, catalog/state/ranking/notifications services, session write throttling, and Prisma transaction topology.
- Built the benchmark harness (scripts/bench/loadtest.ts): N virtual players register through the REAL initData exchange (isolated 9100027… range, self-healing), loop a weighted steady-state mix (state 14% · city 14% · ranking 12% · resources/notifications/season 10% each · catalogs 9% each · auth/me 7% · train+cancel 5% — real economy transactions), with per-player IPs, think-time and only-op knobs, warmup pass, p50–p99 latency recording, and /proc RSS/CPU sampling of the server process. Plus diagnostics: concurrency escalation, raw multi-process SQLite floor, write probes, fsync probe.
- BASELINE FINDING (catastrophic, structural): with ≥4 concurrent Prisma interactive transactions the server collapsed — 30–634 s requests, 271 P1008 socket-timeouts in one window, even the notification worker starved. Root cause chain proven by elimination: (a) raw multi-process SQLite (WAL) executes the same concurrent BEGIN IMMEDIATE workloads in ≤0.1 ms → not the database; (b) fsync measured at 0.01 ms → not the disk; (c) single 99 ms write cycle idle vs 11 s under pure read load → engine-level serialization. Read-only projections wrapped in $transaction were the worst offenders (BEGIN IMMEDIATE takes SQLite's RESERVED write lock).
- FIX 1 — transaction-free read paths: resolveSeasonState() resolves the clock state machine WITHOUT an interactive transaction (guarded conditional updateMany, re-read on lost race); getSeasonView/getSeasonRankingView run as plain statements. Tx callers keep resolveSeasonStateInTx inside their own transactions.
- FIX 2 — dedicated write engine: src/lib/db.ts exports dbWrite (separate PrismaClient/engine); ALL 16 interactive-transaction call sites (economy, registration, session exchange, settlement, admin services, notification inbox, equipTitle) now run on it, isolating the write path from read-engine queue floods. runEconomyTransaction additionally takes a process-wide db:write mutex (lock order wallet → db:write) matching SQLite's single-writer reality — measured: 8 parallel write cycles 95,371 ms → 780 ms (122×); concurrency=4 knee 25.7 s → 0.52 s.
- FIX 3 — state N+1: /player/state consolidated from 17 to 7 queries (one parallel read round for player+wallet+city, army stacks, buildings, technologies; energy resolved from the loaded row with a write only on elapsed regen; power aggregated via the new pure computePowerFromRows, shared with computePlayerPower). Profile/statistics paths slimmed the same way. Measured p50 206 → 133 ms at load.
- FIX 4 — memoized catalog views: getArmyCatalogView/getBuildingCatalogView are pure static-config projections → computed once per process (regression-tested identity + shape). Both endpoints now cost auth-only.
- FIX 5 — WAL journal mode enabled persistently on the SQLite file (readers never block the writer) + V6 invariant added to db:verify so fresh databases self-heal to WAL. Connection pool tuned via DATABASE_URL (?connection_limit=40&pool_timeout=10) after the default pool measurably saturated at 40 RPS.
- Capacity ladder (final, all zero-error unless noted): 30p×1.5s think → 18.9 RPS, reads p50 11–22 ms, write cycle p50 121 ms; 50p×1s → 39.7 RPS, writes p99 1.96 s; 100p×2s → 37.2 RPS, reads p99 ≤ 4 s, writes p99 6.2 s; 200p×3s → 26.7 RPS with read bursts hitting the 30 s client cap (5.7% aborts) and write cycles 23 s (bounded FIFO); 200p open-loop → hard saturation at ~44 RPS. RSS 430 MB idle → 1.4 GB worst-case open-loop; CPU 0–2% idle → ~1 core at 100p.
- Verified-correct & left alone (no premature optimization): auth identity reads (ban/role must be re-read per request — security rail), ranking counts (indexed, cheap), wallet reads (ledger-first authority), fsync cost (free on this disk), session write throttling (already 1/min).
- KNOWN ISSUE (documented, not fixable in this sandbox): prisma/schema.prisma line 156 is corrupted (`@@index(onor])` should be `@@index([honor])`) — the DB already carries the correct players_honor_idx from the baseline migration and prisma validate passes, but the sandbox file-sync layer reverts every edit to this file toward HEAD within milliseconds (verified repeatedly; one-line fix documented in the report §7).
- Quality gate (all green): unit 267/267 (+6 perf-caching regression tests) · integration 243/243 · e2e 27/27 → 537 total · tsc clean · eslint clean · db:verify invariants hold (with V6) · dev server healthy; Agent Browser verified the live console (Phase 25 badge/version 0.14.0-phase25, roadmap rows 24–25 COMPLETE, deliverables card refreshed, DATABASE UP 3–17 ms, zero console errors, mobile 390px clean, footer pushes naturally).
- Wrote docs/PHASE25-PERFORMANCE-REPORT.md (executive summary, methodology, checklist coverage incl. honest N/A for battle/market/map APIs, query audit table, index review, caching decisions, capacity ladder, limitations + follow-up levers) and kept the raw benchmark artifacts under docs/bench/.

Stage Summary:
- Phase 25 exit criteria met: latency/query/N+1/memory/CPU/leaderboard/indexes audited with measurements; caching added ONLY where measured and staleness-free; load-test scenarios committed as reusable harness; the one catastrophic bottleneck (122×) and the hot-path N+1 (17→7) fixed with before/after evidence; stable/predictable envelope mapped honestly (≈100 players zero-error on sandbox hardware; production build + PostgreSQL carries the MVP's 200-player target).
- Awaiting approval for the next phase. NO further-phase work started.

---
Task ID: 10
Agent: Z.ai Code (orchestrator — Release/DevOps Engineer role)
Task: PHASE 26 — Production Deployment (Vercel/Render/Koyeb/Railway targets · Supabase/PostgreSQL · Dockerfile · env docs · production config · Telegram webhook/MiniApp/commands · /health + /ready · production-safe migrations & logging · secrets hygiene · deployment guide · production build + smoke test + health check)

Work Log:
- Full repo inspection first: git log confirmed Phases 0–7 + 20–25 committed; enumerated existing health module (/api/health), telegram layer (initData + sendMessage only — NO webhook), env config layer, structured logger, migrations layout (6 SQLite migrations, provider lock "sqlite").
- SOLVED the Phase 25 "unfixable" schema mystery: hexdump of the real bytes proved prisma/schema.prisma line 156 was NEVER corrupted — the sandbox terminal/file-view layer renders `([h` as `(o` (display ghost). `git diff` empty + prisma validate passing + od bytes `@@index([honor])` are the evidence. The PG twin generated from the real bytes carries the correct index; the deploy-artifacts regression test now locks `players_honor_idx` so the ghost can never mask a real defect.
- Production migrations: created `prisma/postgres/schema.prisma` (provider postgresql) + committed additive baseline `prisma/postgres/migrations/00000000000000_init/migration.sql` (1,338 lines · 54 CREATE TABLE · 82 indexes incl. players_honor_idx · zero destructive DDL) + `migration_lock.toml`. Generated via `prisma migrate diff --from-empty --to-schema-datamodel` and validated (`prisma validate` green). Scripts: `db:pg:validate`, `db:pg:deploy`, `db:pg:baseline`. Policy: deploy runs `migrate deploy` only (recorded, append-only, no-op safe); future changes land as NEW reviewed migrations.
- Health probes: `/health` liveness (dependency-free, no-store, 200) · `/ready` readiness (hard-timeboxed 2 s SELECT 1 through the game's Prisma path + prod-only required-secret gate → 503 when not ready; worker registration reported informationally, never gating) · `/api/health` kept for the console. New `probeLiveness`/`probeReadiness` in the health service; worker exposes `isNotificationWorkerActive()`.
- Production-safe logging: `src/lib/db.ts` read client now gates per-query logging on `isProd` (query logs existed unconditionally — would flood prod streams and leak query shapes/params). Logger itself already production-grade (JSON lines, sensitive-key redaction, LOG_LEVEL); verified and documented, including the webhook privacy rule (update_id + command word only, never message text).
- Telegram bot production surface (TELEGRAM_ARCHITECTURE.md §2–3 honored): NEW pure router `src/lib/telegram/bot.ts` (BOT_COMMANDS = the five REAL data-backed commands start/help/play/profile/rank — unshipped systems deliberately NOT advertised; Mini App inline keyboard; 4096-cap truncation; plain-text replies so names can never inject markup; reserved `ref_`/`camp_` start-payload grammar parsed and safely ignored until its phase); NEW webhook pipeline `webhook.service.ts` (constant-time secret compare via sha256+timingSafeEqual → 401; 64 KiB bounded read; zod update envelope; garbage acked 200 to prevent retry storms; RETRYABLE send failures → 503 so Telegram redelivers, permanent → 200 + ops log; data loaders read the REAL player/wallet/ranking paths); route `POST /api/v1/telegram/webhook` (deliberately outside the envelope — machine caller contract); `sendTelegramMessage` gained optional reply_markup (notification path unchanged).
- Bot setup tooling: `scripts/telegram/setup-bot.ts` — one command runs getMe → setWebhook(url=APP_URL/api/v1/telegram/webhook, secret_token, allowed_updates=[message,callback_query]) → setMyCommands → setChatMenuButton(web_app→APP_URL) → getWebhookInfo verification; `--status` / `--delete` (rollback) / `--drop-pending`; https enforced in prod; npm scripts `telegram:setup|status|teardown`.
- Env & secrets: `.env.example` recreated with the FULL contract (all validated env keys incl. new PORT/HOSTNAME + RUN_MIGRATIONS ops key, REQUIRED-IN-PROD markers, Supabase direct-vs-pooled guidance); `.gitignore` now tracks `.env.example` explicitly (`!.env.example` — it was silently ignored by `.env*`); dev `.env` restored to LABELED dev-only placeholders (sandbox reset had wiped the test creds — integration suites hard-require them); secrets scan of staged tree clean.
- Container & parity: multi-stage `Dockerfile` (deps → prod-deps with POSTGRES prisma generate → build with type errors failing the build → runner: standalone + static + public + prod node_modules + prisma/postgres, non-root `bun` user, EXPOSE 3000, HEALTHCHECK on /health, entrypoint `RUN_MIGRATIONS=true` → `prisma migrate deploy` then exec for signal-safe PID 1) + `.dockerignore` (secrets/DB/dev artifacts excluded) + `docker/docker-entrypoint.sh` + `docker-compose.yml` local PG-parity stack.
- Docs: `DEPLOYMENT.md` — architecture, artifact map, env table, secrets policy, probe semantics, walk-throughs (container platforms / Vercel with honest serverless caveats + cron-driven notification tick / compose parity), Supabase specifics (direct 5432 for migrate, pooled 6543 + pgbouncer flag for runtime, RSL off, backups), migration workflow, Telegram go-live, logging, rollback, scaling (Phase 25 link), go-live checklist, honest limitations register. API index route + console page updated (Phase 26 row, deliverables, badge, v0.15.0-phase26).
- Test-infra incident during bring-up: the sandbox workspace reset had wiped the untracked SQLite dev DB (db/ gone) → rebuilt deterministically from the committed migration chain (`prisma migrate deploy`) + seed + db:verify invariants ✓; also restored the labeled dev .env placeholders. Both recovery paths are exactly what a fresh clone/CI would do.
- Quality gate (all green): tsc --noEmit ✓ · eslint ✓ · prettier ✓ · unit 294/294 (+27) · integration 257/257 (+14) · e2e 30/30 (+3) → 581 tests / 0 failures · db:pg:validate ✓ · db:verify ✓ · PRODUCTION BUILD ✓ (standalone, /health /ready /api/v1/telegram/webhook all registered as dynamic routes).
- Smoke test on the standalone production server (port 3100, prod env, real dev DB): /health 200 dependency-free ✓ · /ready 200 {db up 4 ms, config up, worker up} ✓ · /api/health envelope healthy v0.15.0-phase26 ✓ · console 200 HTML ✓ · security headers incl. production-only HSTS ✓ · /api/v1/auth/me anonymous 401 ✓ · webhook without secret → 401 FORBIDDEN ✓ · webhook with secret + /help → full pipeline: command routed, REAL network call to api.telegram.org with the placeholder token classified PERMANENT (Not Found) → ack 200, privacy-compliant logging (update_id + command word only) ✓.

Stage Summary:
- Phase 26 exit criteria met: deployable via Dockerfile on Render/Koyeb/Railway and via Vercel (caveats documented); Supabase/PostgreSQL production migrations committed and validated; /health + /ready (+ existing /api/health) live in build and smoke-verified; Telegram webhook + Mini App menu button + real commands configurable with one command; production-safe migrations (additive-only, deploy-driven) and logging (query noise off, redaction on, no PII) enforced in code; env contract fully documented with zero secret material tracked in Git; complete DEPLOYMENT.md.
- Key decisions: prisma/postgres/ is the production schema source of truth with a parity regression test guarding drift; the bot advertises only real data-backed commands (honesty rule); webhook uses Telegram-native status codes instead of the API envelope; readiness never gates on the informational worker flag.
- Corrected record: the Phase 25 "known issue" about schema.prisma line 156 was a sandbox display artifact, not a repository defect (evidence in this entry).
- Awaiting approval for the next phase. NO further-phase work started.

---
Task ID: 11
Agent: Z.ai Code (orchestrator — Release QA Auditor role)
Task: PHASE 27 — Final Production QA (whole-project audit across 27 areas · critical user journey E2E · fix critical bugs · full regression · honest release verdict)

Work Log:
- Full repo inspection first: git log confirmed Phases 0–26 committed; enumerated the complete surface (55 API routes, 16 services, 54 tables, frontend feature clients + 1,429-line admin panel) and mapped all 27 requested areas to their true implementation state.
- NEW release-acceptance suite tests/integration/deploy/release-journey.test.ts (8 tests, one continuous session): bot /start via the REAL webhook pipeline (secret-gated, transport injected) answers the greeting + PLAY web_app button → initData HMAC exchange bootstraps player/city/wallet (starter gold asserted exactly) → /auth/me → city roster → resources + ledger (bootstrap faucet is a real ledger row) → REAL 12s Farm construction (exact debits, early-claim 409, +20 season points) → REAL 22s swordsman training (exact debits, early-claim 409, roster 20→21) → live season standing 22 pts + present in LIVE ranking → CONSTRUCTION_COMPLETE + TRAINING_COMPLETE delivered → mark-read idempotent. Attack/Battle step recorded as honest UNAVAILABLE evidence (no battle route exists — engine pending).
- Authoring fixed five wrong assumptions in the NEW test itself (response field names resources/entries/units/unreadCount, 22s-per-unit training window, starter roster of 20) — product behaved correctly in every case; no product bug found.
- Agent-Browser LIVE verification (zero console errors end-to-end): anonymous console → dev session → SIGNED IN + PLAYER STATE LIVE → Town Hall upgrade via UI → second upgrade correctly refused with visible typed BUILDING_QUEUE_BUSY (1/1) → FINISH claim via UI → MY SEASON POINTS 20, rank #1 → notification bell 1 unread → mark-all-read → 0 unread → mobile 390px (no horizontal scroll, footer pushes naturally).
- Bug register: zero critical, zero medium open. Three low items resolved/documented: (L-1) .env.example was silently untracked (fixed Phase 26, tested now), (L-2) Phase 25's "corrupted schema line" proven a sandbox display ghost (corrected record), (L-3) integration-vs-live-server notification drain race documented as test-infra guidance (product claims are atomic count===1).
- Console metadata updated (no features): v0.16.0-phase27, PHASE 27 COMPLETE badge, roadmap row 27, deliverables card switched to the Phase 27 verdict entries; webhook test version assertion now imports APP_VERSION instead of a hardcoded string.
- Quality gate (all green, run today): tsc ✓ · eslint ✓ · prettier ✓ · unit 294/294 · integration 265/265 (incl. release-journey 8/8) · e2e 30/30 → 589 tests / 0 failures · db:pg:validate ✓ · db:verify ✓ · production build ✓ (0 errors) · Phase 26 standalone smoke evidence carries over.
- Wrote docs/PHASE27-FINAL-PRODUCTION-REPORT.md: 27-area release matrix (10 systems production-ready with evidence · battle/quests/market/clan/tech/commander/equipment/spy/diplomacy/world-boss/map honestly gated pending their phases) · journey verification records · full bug register · security/performance/deployment status · 7 prioritized next steps · verdict.

Stage Summary:
- Verdict (honest, per the release contract): 🟡 CONDITIONALLY READY — the INFRASTRUCTURE is release-grade (auth, bootstrap, city, economy, army, seasons, notifications, admin, security, deployment all carry working-software evidence), but the GAME is not complete for real players: the PvP core (battle engine) and the remaining player systems are unimplemented, so a tomorrow-launch would be a polished economy/city/army/season sandbox without combat. Recommendation: market-launch after the Battle Engine (Phase 8) + remaining systems; a closed technical alpha can go live via DEPLOYMENT.md as-is.
- Exit criteria met: full audit performed, journey verified at both API and UI level, all regression suites green (589), build/lint/typecheck/format green, final report issued with an evidence-traceable matrix and an honest limitations list. NO new features were added.
- Awaiting approval for the next phase. NO further-phase work started.

---
Task ID: 12
Agent: Z.ai Code (orchestrator — Combat Systems Engineer role)
Task: PHASE 28 — Complete Battle Engine (attack → combat → casualties → rewards → ranking → notification → history, server-authoritative, integrated into the existing architecture)

Work Log:
- Full repo inspection FIRST: git log confirmed Phases 0–27 committed; enumerated Prisma schema (Battle/BattleRound/BattleLog/March/ScoutReport/IdempotencyKey tables already in the committed baseline), economy service (BATTLE_REWARD ledger reason pre-declared), energy lazy-tick, notification catalog (ATTACK_RESULT payload + dedupe keys pre-declared in Phase 22), admin battle inspector, route/guard/mutex/transaction patterns. Existing types/battle.ts design contract discovered and honored.
- New src/lib/game/config/battle.ts — THE balance authority: versioned snapshot (version 1), maxRounds 12, variance ±1500 bps, class initiative, counter edge clamps, loot policy (30% lootable, carry-capped), casualty/hospital policy, energy cost 10, cooldown 60s, protection rules (newbie level<5 OR age<48h · inactive 7d · level gap 25 · 5 raids/target/24h), terrain table, rewards (honor/xp/season, never negative), idempotency TTL; self-validating invariants.
- New PURE simulator src/lib/game/engine/battle/simulator.ts: seeded mulberry32 PRNG (crypto seed from service), deterministic targeting (counter → lowest HP → order), initiative-ordered rounds (attacker first; battles may end mid-round), defense-divisor damage, catalog-driven counters (weakAgainst stored negative, summed — clamped), carry-capped BigInt loot with direct-ratio scaling, hospital share machinery, max-rounds HP tiebreak. NO I/O, NO clock, NO Math.random.
- New src/lib/game/services/battle.service.ts — the single combat write path: idempotency fast-path + in-tx claim (action BATTLE_ATTACK, request-hash, 24h TTL, original-response replay); target/season/cooldown/protection validation (typed zero-write errors); lazy energy sync + CAS decrement (energy < 0 structurally impossible); real-army loading incl. defender WALL level → defense modifiers; unguarded-city rule (auto win, carry-capped loot, zero rounds); Battle + BattleRound + BattleLog rows; CAS-guarded unit decrements; ledger loot transfer both sides (BATTLE_REWARD, refType battle); honor/XP (grantXp CAS)/season points (awardSeasonPointsInTx, source BATTLE_VICTORY — union extended); append-only statistics both players; power recalculation both players; ATTACK_RESULT notifications ×2 via the existing outbox. Runs behind battle:engine → db:write mutexes (deadlock-free with the house lock order; CAS is the multi-instance backstop).
- API (4 routes, defineRoute + requirePlayer + envelope): POST /api/v1/battles/attack · GET /api/v1/battles/targets (public roster, armies NEVER included, protection reasons vs caller) · GET /api/v1/battles (history, both roles, caller-perspective outcome) · GET /api/v1/battles/[id] (participant-only detail with round trace). New error code SEASON_NOT_ACTIVE.
- Mini App: new feature client src/features/battle (types + TanStack Query hooks, idempotency key = crypto.randomUUID per attack, full invalidation fan-out); Battle Engine card in page.tsx (readiness strip · target roster with protection badges · two-step ATTACK → CONFIRM · result panel VICTORY/DEFEAT/DRAW with casualties/loot/xp/honor/season/seed · expandable history with round-by-round trace); roadmap row 28 COMPLETE, deliverables card, badge, footer, v0.17.0-phase28.
- Tests: 27 unit (simulator determinism/variance/outcomes incl. max-rounds DRAW/counters both directions/casualty+loot invariants/hospital/validation/modifier extension point + config invariants); 18 integration (security matrix: unauth/self/ghost/banned/protected/repeated-raid/energy/cooldown/empty-army/garbage; full winning pipeline asserting battle row, rounds, CAS casualties, ledger both sides, honor/XP/season/stats/power/logs/notifications/history/detail authorization; unguarded city; idempotency replay + key conflict; parallel triple-submit ⇒ exactly one battle; two attackers one defender; energy non-negativity; targets hidden-army guarantee); 9 E2E journey (real initData auth → scout targets → attack → immutable row → both perspectives → round report → ledger/statistics → LIVE season ranking → both ATTACK_RESULT notifications → cooldown gate). Release-journey STEP 8 upgraded from "engine pending" evidence to REAL attack-route round-trip (protected-target refusal; exact season arithmetic preserved). Isolated 9100028/9100029 bench ranges with Restrict-aware purge.
- Benchmarks (docs/bench/battle-{100p,500p}p.md): 100 concurrent → 100/100 resolved, 0 errors, p50 2.8s p95 4.7s, 20.3/s; 500 concurrent → 500/500 resolved, 0 errors, p50 15.2s p95 24.3s, 19.7/s; ~20 full-pipeline battles/s per instance bounded by the deliberately serialized write path (Phase 25 architecture); zero inconsistencies under full fan-out.
- LIVE Agent-Browser verification (zero console errors): dev session → battle card renders → newbie-shielded targets visibly disabled vs attackable lv23 target → two-step confirm → 🏆 VICTORY (seed displayed) → loot/honor/xp/season on the result panel → history + round-by-round REPORT with correct casualty attribution → energy 100→81, cooldown state → mobile 390px no horizontal overflow → sticky footer.
- LIVE verification caught and fixed TWO real bugs: (1) opponent resolution inverted in history/detail (attacker saw their own name as opponent) — fixed + regression assertions added; (2) round-record casualty attribution inverted (attacker row listed defender stack ids) — simulator round loop restructured to owner-keyed accumulators; zero-count loss entries filtered.
- Quality gate (all green, run today): tsc ✓ · eslint ✓ · prettier ✓ · unit 321/321 · integration 283/283 (incl. upgraded release journey 8/8) · e2e 39/39 → 643 tests / 0 failures (baseline was 589) · production build ✓ (battles routes in the standalone manifest) · /health + /ready report v0.17.0-phase28 phase 28 · dev.log clean (only the documented transient worker timeout during the 500-way bench burst, retried).

Stage Summary:
- The Phase 27 verdict's missing core system is CLOSED: a real player can now attack → combat resolves deterministically server-side → casualties apply with CAS invariants → loot crosses the ledger on both sides → honor/XP/season ranking update → both participants are notified → history and round-level reports are queryable — one atomic transaction per battle, idempotent, race-safe, protection-gated.
- Nothing duplicated, nothing rebuilt: the engine consumes the Phase 2 battle contract tables, the Phase 5 ledger, the Phase 4 energy/progression/power/statistics services, the Phase 20 season engine, the Phase 22 notification outbox and the Phase 21 admin inspector. Schema unchanged; PG parity untouched (no migration needed).
- Honest extension points documented (commanders/equipment/technology/morale/formation/terrain/hospital/async marches) in docs/BATTLE-ENGINE.md — declared, not faked.
- Known limitations: synchronous resolution (March table reserved for a future travel-time system); ~20 battles/s per instance on the serialized SQLite write path (PG guidance in DEPLOYMENT.md); single-node in-process mutexes (CAS remains the correctness backstop).
- Awaiting approval for the next phase. NO further-phase work started.
