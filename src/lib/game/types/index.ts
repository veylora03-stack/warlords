/**
 * WARLORDS — Domain type contracts (barrel).
 * Phase 0 baseline: these types are the authoritative language shared by
 * engines, services, and the HTTP layer. They grow with each phase.
 */

export * from './common'
export * from './game-state'
export * from './battle'
